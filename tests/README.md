# Автотесты для админ-панели

## Описание

Набор автотестов для проверки функциональности админ-панели Vital Bot. Тесты проверяют:

- ✅ Авторизацию и доступ
- ✅ Страницы админ-панели
- ✅ Функциональность редактирования товаров
- ✅ JavaScript функции и обработчики событий
- ✅ API endpoints

## Запуск тестов

### Локально (если сервер запущен на localhost:3000)

```bash
npm run test:admin
```

или

```bash
node tests/admin.test.js
```

### На продакшене (Railway)

```bash
npm run test:admin:prod
```

или

```bash
TEST_BASE_URL=https://vital-production-82b0.up.railway.app ADMIN_PASSWORD=ваш_пароль node tests/admin.test.js
```

### С кастомными параметрами

```bash
TEST_BASE_URL=http://localhost:3000 ADMIN_PASSWORD=admin123 node tests/admin.test.js
```

## Переменные окружения

- `TEST_BASE_URL` - URL сервера для тестирования (по умолчанию: `http://localhost:3000`)
- `ADMIN_PASSWORD` - Пароль администратора (по умолчанию: `admin123`)

## Список тестов

1. **Страница логина доступна** - Проверяет доступность страницы входа
2. **Неверный пароль отклоняется** - Проверяет защиту от неверного пароля
3. **Правильный пароль принимается** - Проверяет успешную авторизацию
4. **Доступ к админке без авторизации блокируется** - Проверяет защиту страниц
5. **Доступ к админке с авторизацией разрешен** - Проверяет доступ после авторизации
6. **Страница товаров доступна** - Проверяет доступность страницы товаров
7. **На странице товаров есть кнопки редактирования** - Проверяет наличие UI элементов
8. **JavaScript функции редактирования определены** - Проверяет наличие JS функций
9. **Модальное окно редактирования присутствует в коде** - Проверяет наличие модального окна
10. **Обработчик событий для кнопок редактирования настроен** - Проверяет event handlers
11. **Страница product2 доступна** - Проверяет альтернативную страницу товаров
12. **API категорий доступно** - Проверяет API endpoint

## Результаты

Тесты выводят результаты в консоль с цветовой индикацией:
- ✅ Зеленый - тест пройден
- ❌ Красный - тест провален
- 📊 Статистика в конце

## Добавление новых тестов

Для добавления нового теста используйте метод `test()` класса `TestRunner`:

```javascript
runner.test('Название теста', async () => {
  // Ваш код теста
  const response = await makeRequest(`${BASE_URL}/admin/endpoint`);
  if (response.statusCode !== 200) {
    throw new Error('Тест провален');
  }
});
```

## Структура файлов

- `tests/admin.test.js` - Основной файл с тестами
- `tests/run-tests.sh` - Bash скрипт для запуска (опционально)
- `tests/README.md` - Документация
