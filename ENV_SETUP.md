# 🔧 Настройка переменных окружения

## Быстрый старт

1. **Скопируйте файл примера:**
   ```bash
   cp .env.example .env
   ```

2. **Заполните обязательные переменные в файле `.env`**

3. **Для Railway/других платформ:** Добавьте переменные в настройках проекта

---

## 📋 Список обязательных переменных

### 1. BOT_TOKEN
**Описание:** Токен Telegram бота  
**Как получить:**
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен

**Пример:**
```
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

---

### 2. DATABASE_URL
**Описание:** Строка подключения к PostgreSQL базе данных

**Формат:**
```
postgresql://username:password@host:port/database
```

**Примеры:**

**Railway:**
```
DATABASE_URL=postgresql://postgres:password@containers-us-west-123.railway.app:5432/railway
```

**Локальная база:**
```
DATABASE_URL=postgresql://postgres:password@localhost:5432/vital_db
```

**Supabase:**
```
DATABASE_URL=postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres
```

**Как создать базу данных:**
- **Railway:** Создайте PostgreSQL сервис в проекте, Railway автоматически создаст переменную `DATABASE_URL`
- **Supabase:** Создайте проект, перейдите в Settings → Database → Connection string
- **Локально:** Установите PostgreSQL и создайте базу данных

---

### 3. ADMIN_EMAIL
**Описание:** Email для входа в админ-панель

**Пример:**
```
ADMIN_EMAIL=admin@vital.com
```

---

### 4. ADMIN_PASSWORD
**Описание:** Пароль для входа в админ-панель  
**⚠️ Используйте надежный пароль!**

**Пример:**
```
ADMIN_PASSWORD=YourSecurePassword123!
```

---

## 📋 Опциональные переменные

### PUBLIC_BASE_URL
**Описание:** Базовый URL вашего приложения  
**Используется для:** Webhooks, ссылок в боте, редиректов

**Пример:**
```
PUBLIC_BASE_URL=https://vital-production.up.railway.app
```

**Для локальной разработки:**
```
PUBLIC_BASE_URL=http://localhost:3000
```

---

### ADMIN_CHAT_ID
**Описание:** Telegram Chat ID администратора для получения уведомлений  
**Можно указать несколько через запятую**

**Как получить:**
1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте команду `/start`
3. Скопируйте ваш `Id`

**Пример:**
```
ADMIN_CHAT_ID=123456789
```

**Несколько администраторов:**
```
ADMIN_CHAT_ID=123456789,987654321,111222333
```

---

### BOT_WEBHOOK_URL
**Описание:** URL для Telegram webhook (если используете webhook вместо polling)  
**Обычно не требуется** - бот работает в режиме polling

**Пример:**
```
BOT_WEBHOOK_URL=https://vital-production.up.railway.app/bot/webhook
```

---

### BOT_WEBHOOK_SECRET
**Описание:** Секрет для верификации Telegram webhook  
**Генерируется автоматически** или создайте случайную строку

**Пример:**
```
BOT_WEBHOOK_SECRET=your-random-secret-string-here
```

---

### SESSION_SECRET
**Описание:** Секрет для шифрования сессий Express  
**⚠️ Измените в production!**

**Пример:**
```
SESSION_SECRET=vital-bot-secret-key-change-in-production
```

**Как сгенерировать:**
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

---

### PORT
**Описание:** Порт, на котором будет работать сервер  
**По умолчанию:** 3000

**Пример:**
```
PORT=3000
```

**Для Railway:** Порт устанавливается автоматически через переменную `PORT`

---

## 💳 Lava Payment (оплата)

### LAVA_SECRET_KEY
**Описание:** Секретный ключ API Lava  
**Как получить:** В личном кабинете Lava → API → Secret Key

**Пример:**
```
LAVA_SECRET_KEY=your_lava_secret_key_here
```

---

### LAVA_BASE_URL
**Описание:** Базовый URL API Lava  
**По умолчанию:** https://gate.lava.top

**Пример:**
```
LAVA_BASE_URL=https://gate.lava.top
```

---

### LAVA_WEBHOOK_SECRET
**Описание:** Секрет для верификации webhook от Lava  
**Как получить:** В личном кабинете Lava → Webhooks → Secret

**Пример:**
```
LAVA_WEBHOOK_SECRET=your_lava_webhook_secret
```

---

## 🎥 Дополнительные настройки

### VIDEO_URL
**Описание:** URL видео для модуля навигации

**Пример:**
```
VIDEO_URL=https://www.youtube.com/watch?v=your_video_id
```

---

### NODE_ENV
**Описание:** Режим окружения  
**Значения:** `development` или `production`

**Пример:**
```
NODE_ENV=production
```

---

## 🚀 Настройка для Railway

1. **Откройте проект в Railway**
2. **Перейдите в Settings → Variables**
3. **Добавьте все обязательные переменные:**
   - `BOT_TOKEN`
   - `DATABASE_URL` (создается автоматически при добавлении PostgreSQL)
   - `ADMIN_EMAIL`
   - `ADMIN_PASSWORD`
4. **Добавьте опциональные переменные по необходимости**

**Railway автоматически использует переменную `PORT`**, не нужно её устанавливать вручную.

---

## ✅ Проверка настроек

После настройки всех переменных, проверьте что приложение запускается:

```bash
npm run build
npm start
```

Если все настроено правильно, вы увидите:
```
Database connected
Initial data ensured
Server is running on port 3000
Bot launched successfully
```

---

## 🆘 Решение проблем

### Ошибка: "Environment variable BOT_TOKEN is required"
**Решение:** Убедитесь, что переменная `BOT_TOKEN` установлена в `.env` файле или в настройках платформы

### Ошибка: "Environment variable DATABASE_URL is required"
**Решение:** 
1. Создайте PostgreSQL базу данных
2. Скопируйте строку подключения
3. Добавьте в переменные окружения

### Ошибка подключения к базе данных
**Решение:**
1. Проверьте правильность строки подключения
2. Убедитесь, что база данных доступна
3. Проверьте firewall/настройки безопасности базы данных

---

## 📚 Дополнительная информация

- Полный список переменных: см. файл `.env.example`
- Документация Prisma: https://www.prisma.io/docs
- Документация Railway: https://docs.railway.app
- Документация Lava: https://lava.top/docs


