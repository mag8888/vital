# 🔍 Проблемы синхронизации между ботом и веб-приложением

## ⚠️ Обнаруженные проблемы

### 1. **Перезапись баланса пользователя** (КРИТИЧНО)
**Место:** `src/services/partner-service.ts:269-272`

**Проблема:**
```typescript
// recalculatePartnerBonuses перезаписывает user.balance
await prisma.user.update({
  where: { id: updatedProfile.userId },
  data: { balance: totalBonus }  // ❌ ПЕРЕЗАПИСЫВАЕТ баланс!
});
```

**Последствия:**
- Когда начисляется 3 PZ за реферала, баланс увеличивается через `increment: 3`
- Потом `recordPartnerTransaction` вызывает `recalculatePartnerBonuses`
- Которая **перезаписывает** баланс на сумму только партнерских транзакций
- Это может привести к потере средств от других источников (пополнения, покупки)

### 2. **Разные источники баланса в боте и веб-приложении**
- **В боте** (партнерский дашборд): показывается `partnerProfile.balance`
- **В веб-приложении** (профиль): показывается `user.balance`
- Эти значения могут расходиться

### 3. **Логика начисления 3 PZ за реферала**
- Начисляется на `user.balance` через `increment: 3`
- Создается транзакция в партнерской программе (если есть профиль)
- Но потом `recalculatePartnerBonuses` может перезаписать баланс

## ✅ Правильная логика

1. **`user.balance`** - общий баланс пользователя (все источники)
2. **`partnerProfile.balance`** - только партнерские бонусы (для отображения)
3. При начислении бонусов:
   - Увеличиваем `user.balance` через `increment`
   - Обновляем `partnerProfile.balance` через пересчет транзакций
   - НЕ перезаписываем `user.balance`!

## 🔧 Исправления

### Исправление 1: Убрать перезапись баланса из recalculatePartnerBonuses
### Исправление 2: Синхронизировать баланс при начислении бонусов через increment
### Исправление 3: Убедиться, что баланс синхронизируется во всех местах



