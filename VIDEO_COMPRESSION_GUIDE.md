# 🎥 Руководство по сжатию видео для Telegram бота

## 📏 Ограничения Telegram

### Размер файла:
- **Максимум:** 50 МБ для ботов
- **Рекомендуется:** 10-20 МБ

### Формат и качество:
- **Форматы:** MP4, AVI, MOV, WMV, FLV, WebM, MKV
- **Лучший формат:** MP4 (H.264 + AAC)
- **Разрешение:** до 1280x720 (720p)
- **Битрейт:** 2-4 Мбит/с
- **Длительность:** до 60 минут (для ботов лучше 30-60 сек)

## 🛠️ Способы сжатия

### 1. Командная строка (ffmpeg)

```bash
# Установка ffmpeg
# macOS:
brew install ffmpeg

# Ubuntu/Debian:
sudo apt install ffmpeg

# Windows:
# Скачайте с https://ffmpeg.org/download.html

# Сжатие видео
ffmpeg -i input.mp4 \
    -c:v libx264 \
    -preset fast \
    -crf 28 \
    -maxrate 2M \
    -bufsize 4M \
    -vf "scale=720:480:force_original_aspect_ratio=decrease,pad=720:480:(ow-iw)/2:(oh-ih)/2" \
    -c:a aac \
    -b:a 128k \
    -movflags +faststart \
    output.mp4
```

### 2. Использование скрипта

```bash
# В папке проекта
./scripts/compress-video.sh input.mp4 output.mp4
```

### 3. Онлайн сервисы

#### Бесплатные:
- **CloudConvert** - https://cloudconvert.com/mp4-converter
- **FreeConvert** - https://www.freeconvert.com/video-compressor
- **Compress Video** - https://compress-video-online.com/

#### Настройки для онлайн сервисов:
- **Качество:** Среднее или Низкое
- **Разрешение:** 720x480 или 1280x720
- **Битрейт:** 1-2 Мбит/с
- **Формат:** MP4

### 4. Десктопные программы

#### Бесплатные:
- **HandBrake** - https://handbrake.fr/
- **VLC Media Player** (встроенный конвертер)
- **Shotcut** - https://shotcut.org/

#### Настройки для HandBrake:
- **Preset:** Fast 720p30
- **Video Codec:** H.264 (x264)
- **Quality:** 22-28 (CRF)
- **Audio Codec:** AAC
- **Audio Bitrate:** 128 kbps

## 📱 Специальные настройки для Telegram

### Для демонстрации продуктов:
```bash
ffmpeg -i input.mp4 \
    -c:v libx264 \
    -preset fast \
    -crf 26 \
    -maxrate 1.5M \
    -bufsize 3M \
    -vf "scale=640:360:force_original_aspect_ratio=decrease" \
    -c:a aac \
    -b:a 96k \
    -t 30 \
    output.mp4
```

### Для коротких анонсов (15-30 сек):
```bash
ffmpeg -i input.mp4 \
    -c:v libx264 \
    -preset fast \
    -crf 24 \
    -maxrate 2M \
    -bufsize 4M \
    -vf "scale=720:480:force_original_aspect_ratio=decrease" \
    -c:a aac \
    -b:a 128k \
    -t 30 \
    output.mp4
```

## 🎯 Рекомендации по контенту

### Для бота Vital:
1. **Длина видео:** 15-60 секунд
2. **Разрешение:** 720x480 или 640x360
3. **Размер:** 5-15 МБ
4. **Формат:** MP4
5. **Аудио:** AAC, 96-128 kbps

### Типы контента:
- **Демонстрация продукта:** 30-45 сек
- **Объяснение технологии:** 45-60 сек
- **Краткий анонс:** 15-30 сек
- **Отзывы клиентов:** 20-40 сек

## ⚡ Быстрые команды

### Проверить размер файла:
```bash
ls -lh video.mp4
```

### Проверить информацию о видео:
```bash
ffprobe -v quiet -print_format json -show_format -show_streams video.mp4
```

### Автоматическое сжатие до нужного размера:
```bash
ffmpeg -i input.mp4 -fs 20M -c copy output.mp4
```

## 🚨 Важные замечания

1. **Всегда сохраняйте оригинал** видео перед сжатием
2. **Тестируйте качество** на разных устройствах
3. **Проверяйте размер** перед загрузкой в бота
4. **Используйте быстрый пресет** для экономии времени
5. **Добавляйте `-movflags +faststart`** для быстрой загрузки
