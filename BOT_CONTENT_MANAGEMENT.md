# Модуль управления контентом бота

## 🎯 Описание

Модуль управления контентом бота позволяет редактировать все тексты и сообщения бота через админ панель без необходимости изменения кода.

## 🗄️ База данных

### Модель BotContent

```prisma
model BotContent {
  id          String   @id @default(auto()) @map("_id") @db.ObjectId
  key         String   @unique // Уникальный ключ для контента
  title       String   // Название контента для админки
  content     String   // Основной контент
  description String?  // Описание для админки
  category    String?  // Категория контента
  isActive    Boolean  @default(true)
  language    String   @default("ru") // Язык контента
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
```

## 📝 Предустановленный контент

### Сообщения (messages)
- `welcome_message` - Приветственное сообщение
- `partner_intro` - Введение в партнёрскую программу
- `support_message` - Сообщение поддержки
- `cart_empty_message` - Корзина пуста
- `order_success_message` - Заказ успешно создан

### Описания (descriptions)
- `about_text` - О проекте
- `direct_plan_text` - Прямая комиссия 25%
- `multi_plan_text` - Многоуровневая система

## 🔧 Использование в коде

### Получение контента

```typescript
import { getBotContent } from '../services/bot-content-service.js';

// Получить контент по ключу
const welcomeMessage = await getBotContent('welcome_message');

// Получить контент с fallback
const aboutText = await getBotContent('about_text') || fallbackText;
```

### Инициализация контента

```typescript
import { initializeBotContent } from '../services/bot-content-service.js';

// Инициализировать базовый контент
await initializeBotContent();
```

## 🎛️ Админ панель

### Доступ к управлению контентом

1. Откройте админ панель: `http://localhost:3000/admin`
2. Войдите с учетными данными администратора
3. Найдите раздел "BotContent" в списке ресурсов
4. Нажмите на "BotContent" для управления

### Редактирование контента

1. **Просмотр списка**: Все контенты отображаются с ключом, названием, категорией и языком
2. **Редактирование**: Нажмите на любой контент для редактирования
3. **Создание нового**: Нажмите "Create new" для добавления нового контента
4. **Фильтрация**: Используйте фильтры по категории, языку и активности

### Поля для редактирования

- **Key** - Уникальный ключ (например: "welcome_message")
- **Title** - Название для админки (например: "Приветственное сообщение")
- **Content** - Основной контент (поддерживает HTML и эмодзи)
- **Description** - Описание для админки
- **Category** - Категория (messages, descriptions, buttons)
- **Language** - Язык (ru, en)
- **Is Active** - Активен ли контент

## 📋 Примеры контента

### Приветственное сообщение
```
👋 Добро пожаловать в Vital!

💧 Мы доставляем структурированную воду для вашего здоровья и благополучия.

🛍️ Выберите товары в нашем каталоге или узнайте больше о партнёрской программе!
```

### О проекте
```
🌟 Vital - это революционная технология структурирования воды!

💧 Наша вода проходит специальную обработку, которая:
• Улучшает структуру молекул воды
• Повышает биологическую активность
• Способствует лучшему усвоению организмом

🔬 Научно доказанные преимущества:
• Усиление иммунитета
• Улучшение обмена веществ
• Повышение энергии и жизненного тонуса
• Антиоксидантные свойства

🌱 Экологически чистая технология без химических добавок!
```

## 🔄 Интеграция с модулями

### Модуль About
```typescript
export async function showAbout(ctx: Context) {
  const aboutText = await getBotContent('about_text') || fallbackAboutText;
  await ctx.reply(aboutText, { ...keyboard, parse_mode: 'HTML' });
}
```

### Модуль Partner
```typescript
export async function showPartnerIntro(ctx: Context) {
  const programIntro = await getBotContent('partner_intro') || fallbackProgramIntro;
  await ctx.reply(programIntro, planKeyboard());
}
```

### Модуль Cart
```typescript
if (cartItems.length === 0) {
  const emptyCartMessage = await getBotContent('cart_empty_message') || '🛍️ Ваша корзина пуста';
  await ctx.reply(emptyCartMessage);
  return;
}
```

## 🚀 Развертывание

### 1. Применить изменения схемы
```bash
npx prisma generate
```

### 2. Пересобрать проект
```bash
npm run build
```

### 3. Запустить сервер
```bash
npm start
```

### 4. Инициализировать контент
Контент автоматически инициализируется при первом запуске сервера.

## 🎨 Категории контента

### messages
- Приветственные сообщения
- Уведомления
- Сообщения об ошибках
- Информационные сообщения

### descriptions
- Описания продуктов
- Описания программ
- Информационные тексты
- Объяснения функций

### buttons
- Тексты кнопок
- Названия меню
- Подписи к элементам

## 🌍 Многоязычность

### Поддержка языков
- `ru` - Русский (по умолчанию)
- `en` - Английский
- Можно добавить другие языки

### Использование
```typescript
// Получить контент на русском
const contentRu = await getBotContent('welcome_message', 'ru');

// Получить контент на английском
const contentEn = await getBotContent('welcome_message', 'en');
```

## 🔧 API функции

### getBotContent(key, language?)
Получить контент по ключу и языку.

### getAllBotContents()
Получить все контенты.

### upsertBotContent(data)
Создать или обновить контент.

### deleteBotContent(key)
Удалить контент.

### initializeBotContent()
Инициализировать базовый контент.

## 📊 Мониторинг

### Логи
Все операции с контентом логируются в консоль:
- ✅ Успешное получение контента
- ❌ Ошибки при работе с контентом
- 🔄 Инициализация контента

### Отладка
```typescript
// Проверить, что контент загружается
const content = await getBotContent('welcome_message');
console.log('Content loaded:', content);
```

## 🎯 Преимущества

1. **Без изменения кода** - редактирование через админку
2. **Многоязычность** - поддержка разных языков
3. **Fallback** - резервные тексты в коде
4. **Категоризация** - удобная организация контента
5. **Версионность** - отслеживание изменений
6. **Активность** - возможность отключения контента

## 🔮 Будущие улучшения

- [ ] История изменений контента
- [ ] Предварительный просмотр
- [ ] Массовое редактирование
- [ ] Экспорт/импорт контента
- [ ] Шаблоны контента
- [ ] A/B тестирование
