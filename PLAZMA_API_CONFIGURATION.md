# 🔑 Конфигурация Plazma API - Объяснение переменных

## 📋 Объяснение переменных окружения

### На сервере Plazma (отдающий API):
- **`EXTERNAL_API_KEY`** — переменная окружения, которая хранит ключ для проверки входящих запросов
- Этот ключ используется для валидации заголовка `X-API-Key` от клиентов

### На сервере Vital (наш сервер, клиент):
- **`PLAZMA_API_KEY`** или **`EXTERNAL_API_KEY`** — можно использовать любую из этих переменных
- Значение должно совпадать с ключом, настроенным на сервере Plazma
- Код поддерживает обе переменные: `PLAZMA_API_KEY || EXTERNAL_API_KEY`

## ✅ Это не может вредить

**Почему это безопасно:**

1. **Разные серверы, разные роли:**
   - На Plazma сервере `EXTERNAL_API_KEY` — это их внутренняя переменная
   - На Vital сервере мы используем свою переменную для хранения того же ключа

2. **Код поддерживает оба варианта:**
   ```typescript
   plazmaApiKey: process.env.PLAZMA_API_KEY || process.env.EXTERNAL_API_KEY
   ```
   - Можно использовать любую переменную
   - Главное — правильное значение ключа

3. **Принцип работы:**
   ```
   Vital сервер → Отправляет ключ в заголовке X-API-Key → Plazma сервер проверяет его
   ```

## 🔧 Рекомендации

### На сервере Vital:
Используйте переменную `PLAZMA_API_KEY` (более понятное название для контекста):
```bash
PLAZMA_API_KEY=ваш-ключ-от-plazma-сервера
```

### На сервере Plazma:
Используйте переменную `EXTERNAL_API_KEY` (как настроено):
```bash
EXTERNAL_API_KEY=ваш-секретный-ключ
```

## ⚠️ Важно

**Значение ключа должно быть одинаковым на обоих серверах!**

- Если на Plazma установлен ключ: `abc123xyz`
- То на Vital должен быть тот же ключ: `abc123xyz`
- Название переменной может быть разным, но значение — одинаковое

## 📝 Пример

**Сервер Plazma (.env):**
```bash
EXTERNAL_API_KEY=my-super-secret-key-12345
```

**Сервер Vital (.env):**
```bash
PLAZMA_API_KEY=my-super-secret-key-12345
# или
EXTERNAL_API_KEY=my-super-secret-key-12345
```

Оба варианта работают одинаково! 🎉


