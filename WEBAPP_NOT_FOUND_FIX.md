# 🔧 Исправление ошибки "Not Found" для /webapp

## Проблема

При открытии `https://vital-production-82b0.up.railway.app/webapp` показывается ошибка "Not Found" от Railway.

## Возможные причины

1. **Сервер не запускается** из-за ошибок MongoDB
2. **Файлы webapp не доступны** после сборки
3. **Сервер падает** до того, как роуты регистрируются

## Решение

### Шаг 1: Проверьте логи сервера

1. Railway Dashboard → сервис **`vital`**
2. **Deployments** → последний деплой → **View Logs**
3. Проверьте, что видите:
   - ✅ `Server is running on port XXXX`
   - ✅ `Routes registered`
   - ✅ `Bot launched successfully`

### Шаг 2: Проверьте переменную DATABASE_URL

Если сервер не запускается из-за ошибок MongoDB:

1. **Сервис `vital`** → **Variables**
2. Найдите `DATABASE_URL`
3. Убедитесь, что используется **Reference**: `{{MongoDB.MONGO_PUBLIC_URL}}`
4. Если это обычное значение (не ссылка):
   - Удалите переменную
   - Создайте заново через **Add Reference**
   - Выберите `MongoDB` → `MONGO_PUBLIC_URL`

### Шаг 3: Сбросьте пароль MongoDB

Если ошибки аутентификации продолжаются:

1. **Сервис `MongoDB`** → **Settings**
2. Нажмите **Reset Password**
3. Railway сгенерирует новый пароль
4. `MONGO_PUBLIC_URL` автоматически обновится

### Шаг 4: Перезапустите сервер

1. **Deployments** → **Redeploy**
2. Дождитесь завершения деплоя
3. Проверьте логи

### Шаг 5: Проверьте, что сервер отвечает

1. Откройте: `https://vital-production-82b0.up.railway.app/health`
2. Должно вернуться: `{"status":"ok",...}`
3. Если возвращает ошибку — сервер не запущен

## Ожидаемый результат

После исправления:
- ✅ `/health` возвращает `{"status":"ok"}`
- ✅ `/webapp` показывает веб-приложение
- ✅ Бот отвечает в Telegram

## Если проблема осталась

1. Проверьте, что код задеплоен:
   - Railway → Deployments → последний деплой должен быть недавним
   - Статус должен быть зелёным

2. Проверьте, что порт правильный:
   - Railway автоматически устанавливает `PORT`
   - Сервер должен слушать на `0.0.0.0`, что уже настроено

3. Проверьте логи на наличие ошибок:
   - `Error: ...`
   - `Failed to ...`
   - `Cannot find module ...`





