# ⚡ Быстрая инструкция по деплою Plazma API

## 🎯 Проблема

В логах видно:
```
⚠️ /products endpoint not found, trying /catalog...
❌ Plazma API error: 404 Not Found
```

Это значит, что **внешний API для Plazma еще не задеплоен** на Railway.

## ✅ Что уже готово

Изменения в коде уже сделаны:
- ✅ `src/api/external.ts` - внешний API
- ✅ `src/lib/prisma.ts` - Prisma клиент  
- ✅ `src/config/env.ts` - переменная `EXTERNAL_API_KEY`
- ✅ `src/server.ts` - роутер подключен

## 🚀 Что нужно сделать

### Вариант 1: Plazma деплоится через Railway напрямую

1. Зайдите в **Railway Dashboard**
2. Найдите проект **Plazma**
3. Перейдите в **Variables** (переменные окружения)
4. Добавьте:
   ```
   EXTERNAL_API_KEY=ваш-секретный-ключ-здесь
   ```
   ⚠️ **Важно:** Этот ключ должен совпадать с `PLAZMA_API_KEY` в проекте Vital!

5. Railway автоматически перезапустит проект

### Вариант 2: Plazma деплоится из Git репозитория

Если проект Plazma подключен к отдельному Git репозиторию:

1. Инициализируйте git (если еще не сделано):
   ```bash
   cd /Users/ADMIN/Vital/plazma
   git init
   git remote add origin <URL-вашего-репозитория>
   ```

2. Закоммитьте изменения:
   ```bash
   git add .
   git commit -m "Add external API endpoints for Vital integration"
   git push origin main
   ```

3. Railway автоматически задеплоит изменения

4. Добавьте переменную `EXTERNAL_API_KEY` в Railway Dashboard

## 🔐 Настройка ключей

**В проекте Vital (Railway Dashboard → Vital → Variables):**
```
PLAZMA_API_KEY=ваш-секретный-ключ
PLAZMA_API_URL=https://plazma-production.up.railway.app/api/external
```

**В проекте Plazma (Railway Dashboard → Plazma → Variables):**
```
EXTERNAL_API_KEY=ваш-секретный-ключ
```

**Оба ключа должны быть одинаковыми!**

## ✅ Проверка после деплоя

После деплоя в логах Vital должно появиться:
```
✅ Plazma API response: Found X products
```

Вместо:
```
❌ Plazma API error: 404 Not Found
```

## 📝 Быстрый чеклист

- [ ] Зайти в Railway Dashboard
- [ ] Найти проект Plazma
- [ ] Добавить переменную `EXTERNAL_API_KEY`
- [ ] Убедиться, что ключ совпадает с `PLAZMA_API_KEY` в Vital
- [ ] Перезапустить проект Plazma (или дождаться автоматического деплоя)
- [ ] Проверить логи - должны появиться сообщения о External API
- [ ] Проверить, что ошибка 404 исчезла из логов Vital

## 🆘 Если не работает

1. Проверьте логи Railway для проекта Plazma
2. Убедитесь, что переменная `EXTERNAL_API_KEY` установлена
3. Проверьте URL проекта Plazma - возможно он изменился
4. Обновите `PLAZMA_API_URL` в Vital, если URL изменился













