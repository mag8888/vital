# 🚀 Настройка внешнего API для Plazma

## ✅ Что было сделано

1. **Добавлена переменная окружения** `EXTERNAL_API_KEY` в `src/config/env.ts`
2. **Создан внешний API роутер** `src/api/external.ts` с endpoint'ами:
   - `GET /api/external/catalog` - полный каталог с категориями и товарами
   - `GET /api/external/categories` - список категорий
   - `GET /api/external/products` - список товаров с фильтрацией
   - `GET /api/external/products/:id` - конкретный товар
   - `POST /api/external/orders` - создание заказа
3. **Реализована аутентификация** через заголовок `X-API-Key`
4. **Подключен роутер** к серверу в `src/server.ts`
5. **Обновлены CORS заголовки** для поддержки `X-API-Key`
6. **Создан файл** `src/lib/prisma.ts` для работы с базой данных

## 📋 Что нужно сделать для запуска

### 1. Установить переменную окружения

В Railway (или вашем окружении) добавьте:
```env
EXTERNAL_API_KEY=ваш-секретный-ключ
```

**Важно:** Этот ключ должен совпадать с ключом в проекте Vital (`PLAZMA_API_KEY`).

### 2. Собрать и задеплоить проект

```bash
cd plazma
npm install
npm run build
npm start
```

### 3. Проверить работу API

После деплоя проверьте:

```bash
# Проверка каталога
curl -X GET "https://plazma-production.up.railway.app/api/external/catalog?region=RUSSIA" \
  -H "X-API-Key: ваш-секретный-ключ"

# Проверка товаров
curl -X GET "https://plazma-production.up.railway.app/api/external/products?limit=5&region=RUSSIA" \
  -H "X-API-Key: ваш-секретный-ключ"
```

## 🔐 Аутентификация

Все запросы требуют API ключ в заголовке:
```
X-API-Key: ваш-api-ключ
```

Если ключ не указан или неверный, API вернет:
```json
{
  "success": false,
  "error": "Unauthorized. Invalid or missing API key"
}
```

## 📝 Endpoint'ы

### GET /api/external/catalog
Возвращает все категории с товарами.

**Query параметры:**
- `region` (опционально) - `RUSSIA` или `BALI` (по умолчанию `RUSSIA`)

### GET /api/external/products
Возвращает список товаров.

**Query параметры:**
- `categoryId` (опционально) - ID категории
- `region` (опционально) - `RUSSIA` или `BALI`
- `search` (опционально) - поиск по названию/описанию
- `limit` (опционально) - лимит (макс. 1000)
- `offset` (опционально) - смещение

### GET /api/external/products/:id
Возвращает конкретный товар по ID.

### POST /api/external/orders
Создает заказ.

**Тело запроса:**
```json
{
  "contact": "телефон или @username",
  "items": [
    {
      "productId": "id-товара",
      "quantity": 1,
      "price": 1.2
    }
  ],
  "message": "Дополнительное сообщение (опционально)"
}
```

## 🎯 Интеграция с Vital

После деплоя Plazma API, в проекте Vital:

1. Убедитесь, что `PLAZMA_API_KEY` совпадает с `EXTERNAL_API_KEY` в Plazma
2. Убедитесь, что `PLAZMA_API_URL` указывает на правильный URL:
   ```
   https://plazma-production.up.railway.app/api/external
   ```
3. Товары из Plazma автоматически появятся в секции "Рекомендуем" на сайте Vital

## 🔍 Логирование

API логирует все запросы:
- `🔗 External API: Fetching...` - начало запроса
- `✅ External API: Found...` - успешный ответ
- `❌ External API error...` - ошибки

## ⚠️ Важные замечания

1. API ключ должен быть достаточно сложным для безопасности
2. Все запросы логируются на сервере
3. CORS настроен для разрешения запросов с любого источника (можно ограничить при необходимости)
4. Для MongoDB используется case-sensitive поиск (режим insensitive не поддерживается)













