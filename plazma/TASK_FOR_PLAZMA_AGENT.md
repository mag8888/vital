# 📋 Техническое задание для Plazma агента

## 🎯 Цель

Задеплоить изменения внешнего API в проект Plazma, чтобы товары из Plazma отображались в приложении Vital в секции "Рекомендуем".

## ❌ Текущая проблема

В логах Vital видно:
```
⚠️ /products endpoint not found, trying /catalog...
❌ Plazma API error: 404 Not Found
```

**Причина:** Внешний API для Plazma еще не задеплоен на Railway.

## ✅ Что уже готово в коде

Все изменения в коде уже сделаны:

1. ✅ **`src/api/external.ts`** - создан внешний API роутер с endpoint'ами:
   - `GET /api/external/catalog` - полный каталог с категориями и товарами
   - `GET /api/external/categories` - список категорий
   - `GET /api/external/products` - список товаров с фильтрацией
   - `GET /api/external/products/:id` - конкретный товар
   - `POST /api/external/orders` - создание заказа

2. ✅ **`src/lib/prisma.ts`** - настроен Prisma клиент для работы с базой данных

3. ✅ **`src/config/env.ts`** - добавлена переменная `EXTERNAL_API_KEY`

4. ✅ **`src/server.ts`** - роутер подключен: `app.use('/api/external', externalApiRouter)`

## 📝 Что нужно сделать

### Шаг 1: Проверить, что код задеплоен

Убедитесь, что файлы присутствуют на сервере:
- `src/api/external.ts`
- `src/lib/prisma.ts`
- Обновленный `src/server.ts` (с подключенным роутером)

Если код не задеплоен, задеплойте изменения на Railway.

### Шаг 2: Добавить переменную окружения

В **Railway Dashboard** для проекта Plazma:

1. Перейдите в раздел **Variables** (Переменные окружения)
2. Добавьте новую переменную:
   ```
   EXTERNAL_API_KEY=ваш-секретный-ключ-здесь
   ```

   **Важно:** 
   - Ключ должен быть достаточно сложным (минимум 32 символа)
   - Этот ключ должен совпадать с `PLAZMA_API_KEY` в проекте Vital
   - Пример ключа: `sk_live_EXAMPLE_KEY_xxxxxxxxxxxxxxxxxxxxxxxx`

### Шаг 3: Проверить переменные в проекте Vital

В проекте Vital должны быть установлены (если их нет - добавить):

```
PLAZMA_API_KEY=ваш-секретный-ключ-здесь
PLAZMA_API_URL=https://plazma-production.up.railway.app/api/external
```

**Ключ должен быть одинаковым в обоих проектах!**

### Шаг 4: Перезапустить проект Plazma

1. В Railway Dashboard найдите проект Plazma
2. Нажмите **Redeploy** или **Restart**
3. Дождитесь завершения деплоя (2-3 минуты)

### Шаг 5: Проверить логи после деплоя

После перезапуска проверьте логи Railway для проекта Plazma:

1. Откройте раздел **Deployments**
2. Выберите последний деплой
3. Проверьте логи - должны быть:
   - Сообщения о запуске сервера
   - Отсутствие ошибок подключения к базе данных
   - Сообщения о регистрации маршрутов

### Шаг 6: Протестировать API

После деплоя проверьте работу API:

#### Вариант 1: Через curl
```bash
curl -X GET "https://plazma-production.up.railway.app/api/external/catalog?region=RUSSIA" \
  -H "X-API-Key: ваш-секретный-ключ"
```

#### Вариант 2: Через браузер (если ключ установлен)
Откройте в браузере:
```
https://plazma-production.up.railway.app/api/external/products?limit=5&region=RUSSIA
```

**Ожидаемый результат:**
- Должен вернуться JSON с товарами
- Формат: `{ "success": true, "data": [...] }`

**Если возвращается 401:**
- Проверьте, что заголовок `X-API-Key` правильный
- Проверьте, что ключ совпадает в обоих проектах

**Если возвращается 404:**
- Проверьте, что код задеплоен
- Проверьте, что роутер подключен в `server.ts`
- Проверьте URL (возможно, проект Plazma имеет другой домен)

### Шаг 7: Проверить работу в Vital

После успешного тестирования API:

1. Откройте сайт Vital: https://vital-production-82b0.up.railway.app/webapp
2. Прокрутите вниз до секции "Рекомендуем"
3. Должны отображаться товары из Plazma

**В логах Vital должно появиться:**
```
✅ Plazma API response: Found X products
```

**Вместо:**
```
❌ Plazma API error: 404 Not Found
```

## 🔐 Настройка ключей

### Рекомендуемый формат ключа:
```
EXTERNAL_API_KEY=plazma_sk_live_EXAMPLE_KEY_xxxxxxxxxxxxxxxxxxxxxxxx
```

### Где установить:

**Проект Plazma (Railway Dashboard → Plazma → Variables):**
```
EXTERNAL_API_KEY=plazma_sk_live_EXAMPLE_KEY_xxxxxxxxxxxxxxxxxxxxxxxx
```

**Проект Vital (Railway Dashboard → Vital → Variables):**
```
PLAZMA_API_KEY=plazma_sk_live_EXAMPLE_KEY_xxxxxxxxxxxxxxxxxxxxxxxx
PLAZMA_API_URL=https://plazma-production.up.railway.app/api/external
```

## 📋 Чеклист выполнения

- [ ] Проверено, что код задеплоен (файлы `external.ts`, `prisma.ts` есть на сервере)
- [ ] Добавлена переменная `EXTERNAL_API_KEY` в проект Plazma на Railway
- [ ] Добавлена переменная `PLAZMA_API_KEY` в проект Vital на Railway
- [ ] Добавлена переменная `PLAZMA_API_URL` в проект Vital на Railway
- [ ] Ключи совпадают в обоих проектах
- [ ] Проект Plazma перезапущен
- [ ] В логах Plazma нет ошибок
- [ ] API отвечает на запросы (проверено через curl или браузер)
- [ ] В логах Vital исчезла ошибка 404
- [ ] Товары из Plazma отображаются на сайте Vital

## 🆘 Решение проблем

### Проблема 1: API возвращает 401 Unauthorized

**Решение:**
- Проверьте, что переменная `EXTERNAL_API_KEY` установлена в проекте Plazma
- Проверьте, что ключ в заголовке `X-API-Key` совпадает с `EXTERNAL_API_KEY`
- Проверьте, что ключ совпадает с `PLAZMA_API_KEY` в проекте Vital

### Проблема 2: API возвращает 404 Not Found

**Решение:**
- Проверьте URL проекта Plazma - возможно, он изменился
- Обновите `PLAZMA_API_URL` в проекте Vital, если URL другой
- Проверьте, что роутер подключен в `src/server.ts`: `app.use('/api/external', externalApiRouter)`
- Проверьте, что код задеплоен (файлы `external.ts` присутствуют)

### Проблема 3: Ошибка подключения к базе данных

**Решение:**
- Проверьте, что переменная `DATABASE_URL` установлена в проекте Plazma
- Проверьте, что строка подключения корректна
- Проверьте логи Railway для деталей ошибки

### Проблема 4: Товары не отображаются в Vital

**Решение:**
- Проверьте логи Vital - должны быть сообщения о загрузке товаров
- Проверьте, что `PLAZMA_API_URL` указывает на правильный URL
- Проверьте консоль браузера (F12) на наличие ошибок JavaScript
- Убедитесь, что в Plazma есть активные товары с `availableInRussia: true`

## 📞 Контакты для вопросов

Если возникнут проблемы:
1. Проверьте логи Railway для обоих проектов
2. Проверьте переменные окружения
3. Проверьте, что код задеплоен

## ✅ Критерии успешного выполнения

Задача считается выполненной, когда:

1. ✅ API Plazma отвечает на запросы (не возвращает 404)
2. ✅ В логах Vital нет ошибок при загрузке товаров из Plazma
3. ✅ Товары из Plazma отображаются в секции "Рекомендуем" на сайте Vital
4. ✅ Пользователи могут просматривать и заказывать товары из Plazma через Vital

---

**Время выполнения:** 15-30 минут

**Приоритет:** Высокий













