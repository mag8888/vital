# ⚠️ СРОЧНО: Задеплоить Plazma API

## 🔴 Текущая проблема

В логах Vital видно:
```
❌ Plazma API error: 404 Not Found
⚠️ /products endpoint not found, trying /catalog...
❌ Plazma API error: 404 Not Found
```

**Это означает, что внешний API на Plazma не задеплоен!**

## ✅ Что уже готово в коде

- ✅ Файл `plazma/src/api/external.ts` создан
- ✅ Роутер подключен в `plazma/src/server.ts`
- ✅ Все endpoint'ы реализованы

## 🚀 Что нужно сделать СЕЙЧАС

### 1️⃣ Проверить, что код задеплоен

Зайдите в Railway Dashboard → проект **Plazma**:
- Проверьте, что последний деплой содержит изменения с файлом `external.ts`
- Если нет - сделайте `git push` в репозиторий Plazma

### 2️⃣ Добавить переменную окружения

**Railway Dashboard → Plazma → Variables:**

Добавьте:
```
EXTERNAL_API_KEY=plazma_sk_live_838167d170abcdefghijklmnopqrstuvwxyz
```

**⚠️ ВАЖНО:** Этот ключ должен совпадать с `PLAZMA_API_KEY` в проекте Vital!

### 3️⃣ Проверить переменные в Vital

**Railway Dashboard → Vital → Variables:**

Должны быть:
```
PLAZMA_API_KEY=plazma_sk_live_838167d170abcdefghijklmnopqrstuvwxyz
PLAZMA_API_URL=https://plazma-production.up.railway.app/api/external
```

### 4️⃣ Перезапустить Plazma

**Railway Dashboard → Plazma:**
- Нажмите **Redeploy** или **Restart**
- Подождите 2-3 минуты

### 5️⃣ Проверить в логах

После перезапуска в логах Plazma должно появиться:
```
✅ Plazma API Server is running on port 3000
✅ External API URL: https://plazma-production.up.railway.app/api/external
```

### 6️⃣ Протестировать API

Выполните в терминале:
```bash
curl -X GET "https://plazma-production.up.railway.app/api/external/products?limit=5&region=RUSSIA" \
  -H "X-API-Key: plazma_sk_live_838167d170abcdefghijklmnopqrstuvwxyz"
```

**Ожидаемый результат:** JSON с товарами

**Если получаете 401:** Ключ не совпадает
**Если получаете 404:** API не задеплоен
**Если получаете 503:** Переменная `EXTERNAL_API_KEY` не установлена

## 📊 После успешного деплоя

В логах Vital должно появиться:
```
✅ Plazma API response: Found X products
✅ Loaded X products from Plazma API
```

Вместо:
```
❌ Plazma API error: 404 Not Found
```

## 🆘 Быстрая проверка

1. ✅ Код задеплоен? (проверить в Railway)
2. ✅ `EXTERNAL_API_KEY` установлена в Plazma?
3. ✅ `PLAZMA_API_KEY` установлена в Vital? (совпадает с Plazma)
4. ✅ `PLAZMA_API_URL` установлена в Vital?
5. ✅ Проект Plazma перезапущен?

Если все ✅ - товары должны появиться в секции "Рекомендуем"!

---

**⏱ Время: ~10 минут**













