# 🔧 Быстрое исправление 404 ошибки Plazma API

## Проблема
```
❌ Plazma API error: 404 Not Found
```

## Причины и решения

### ❌ Причина 1: Код не задеплоен

**Решение:**
1. Убедитесь, что файл `plazma/src/api/external.ts` существует
2. Убедитесь, что изменения закоммичены и запушены в репозиторий Plazma
3. Проверьте в Railway, что последний деплой содержит эти файлы

### ❌ Причина 2: Переменная окружения не установлена

**Решение:**
1. Railway Dashboard → Plazma → Variables
2. Добавьте: `EXTERNAL_API_KEY=ваш-ключ`
3. **ВАЖНО:** Ключ должен совпадать с `PLAZMA_API_KEY` в проекте Vital!

**Из логов видно, что в Vital ключ начинается с `838167d170...`**
- Установите такой же ключ в Plazma как `EXTERNAL_API_KEY`

### ❌ Причина 3: Проект не перезапущен

**Решение:**
1. Railway Dashboard → Plazma
2. Нажмите **Redeploy** или **Restart**
3. Подождите 2-3 минуты

## ✅ Проверка после исправления

### 1. Проверить API напрямую:

```bash
curl -X GET "https://plazma-production.up.railway.app/api/external/products?limit=5" \
  -H "X-API-Key: ваш-ключ"
```

**Должен вернуть:** JSON с товарами или 401 (если ключ неправильный)

**Если 404:** API не задеплоен или роутер не подключен

### 2. Проверить в логах Plazma:

После перезапуска должно быть:
```
✅ External API routes registered
```

### 3. Проверить в логах Vital:

Должно появиться:
```
✅ Plazma API response: Found X products
```

## 🎯 Чеклист

- [ ] Файл `plazma/src/api/external.ts` существует
- [ ] Код задеплоен (проверено в Railway)
- [ ] `EXTERNAL_API_KEY` установлена в Plazma
- [ ] `PLAZMA_API_KEY` установлена в Vital
- [ ] Ключи совпадают
- [ ] Проект Plazma перезапущен
- [ ] API отвечает на запросы (проверено через curl)

## ⚡ Самый быстрый способ исправить

1. **Railway Dashboard → Plazma → Variables**
   - Добавить: `EXTERNAL_API_KEY=838167d170...` (полный ключ из Vital)

2. **Railway Dashboard → Plazma → Redeploy**

3. **Подождать 2-3 минуты**

4. **Проверить:**
   ```bash
   curl -H "X-API-Key: 838167d170..." \
     https://plazma-production.up.railway.app/api/external/products?limit=5
   ```

Готово! 🎉













