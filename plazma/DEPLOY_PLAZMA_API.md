# 🚀 Деплой Plazma API для интеграции с Vital

## 📋 Текущая ситуация

Изменения для внешнего API уже сделаны в коде:
- ✅ `src/api/external.ts` - создан внешний API роутер
- ✅ `src/lib/prisma.ts` - настроен Prisma клиент
- ✅ `src/config/env.ts` - добавлена переменная `EXTERNAL_API_KEY`
- ✅ `src/server.ts` - подключен роутер `/api/external`

**Но изменения еще не задеплоены на Railway!**

## 🔧 Шаги для деплоя

### Вариант 1: Если Plazma - отдельный проект на Railway

1. **Зайдите в Railway Dashboard** → проект Plazma
2. **Добавьте переменную окружения:**
   ```
   EXTERNAL_API_KEY=ваш-секретный-ключ-здесь
   ```
   **Важно:** Этот ключ должен совпадать с `PLAZMA_API_KEY` в проекте Vital!

3. **Запустите деплой** (Railway автоматически сделает это при push в git)

4. **Проверьте логи** после деплоя

### Вариант 2: Если Plazma и Vital в одном репозитории

Если проект Plazma находится в той же директории, что и Vital, то:

1. **Закоммитьте изменения в git:**
   ```bash
   cd /Users/ADMIN/Vital
   git add plazma/
   git commit -m "Add external API endpoints for Plazma integration"
   git push
   ```

2. **Railway автоматически задеплоит оба проекта**

3. **Добавьте переменную окружения в Railway** для проекта Plazma:
   ```
   EXTERNAL_API_KEY=ваш-секретный-ключ
   ```

## 🔐 Настройка API ключа

### В проекте Vital:
```
PLAZMA_API_KEY=ваш-секретный-ключ
PLAZMA_API_URL=https://plazma-production.up.railway.app/api/external
```

### В проекте Plazma:
```
EXTERNAL_API_KEY=ваш-секретный-ключ
```

**Оба ключа должны быть одинаковыми!**

## ✅ Проверка после деплоя

После деплоя проверьте работу API:

```bash
# 1. Проверка каталога
curl -X GET "https://plazma-production.up.railway.app/api/external/catalog?region=RUSSIA" \
  -H "X-API-Key: ваш-секретный-ключ"

# 2. Проверка товаров
curl -X GET "https://plazma-production.up.railway.app/api/external/products?limit=5&region=RUSSIA" \
  -H "X-API-Key: ваш-секретный-ключ"
```

Если API работает, вы получите JSON с данными. Если нет - проверьте логи Railway.

## 📝 Что делать сейчас

1. **Определите, как развернут проект Plazma на Railway:**
   - Отдельный сервис? → Зайдите в Railway Dashboard и найдите проект Plazma
   - Вместе с Vital? → Проверьте, есть ли отдельный сервис для Plazma

2. **Добавьте переменную окружения** `EXTERNAL_API_KEY` в проект Plazma на Railway

3. **Запустите деплой** или дождитесь автоматического деплоя

4. **Проверьте логи** после деплоя - должны появиться сообщения о запуске External API

## 🐛 Отладка

Если после деплоя API все еще не работает:

1. **Проверьте логи Railway** для проекта Plazma
2. **Проверьте, что переменная `EXTERNAL_API_KEY` установлена**
3. **Проверьте URL проекта Plazma** - возможно, он изменился
4. **Обновите `PLAZMA_API_URL` в проекте Vital**, если URL изменился

## 📞 Что дальше

После деплоя Plazma API:
- ✅ Товары из Plazma автоматически появятся в секции "Рекомендуем" на сайте Vital
- ✅ Пользователи смогут заказывать товары из Plazma через Vital
- ✅ Заказы будут автоматически создаваться в Plazma













