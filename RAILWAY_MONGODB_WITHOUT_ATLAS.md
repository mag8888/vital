# 🔧 Использование Railway MongoDB без Atlas

## ⚠️ Проблема

Prisma требует **replica set** для операций `update()` и `create()`, но Railway MongoDB по умолчанию не поддерживает replica set.

## ✅ Решение: Настроить Railway MongoDB как replica set

Railway MongoDB можно настроить как replica set, но это требует дополнительной конфигурации.

### Вариант 1: Настройка через MongoDB Shell (Рекомендуется)

1. **Откройте MongoDB сервис на Railway:**
   - Railway Dashboard → ваш проект → сервис **MongoDB**
   - Откройте **"Data"** или **"Connect"**

2. **Подключитесь к MongoDB через Railway CLI:**
   ```bash
   railway link
   railway run mongosh
   ```

3. **Инициализируйте replica set:**
   ```javascript
   rs.initiate({
     _id: "rs0",
     members: [
       { _id: 0, host: "localhost:27017" }
     ]
   })
   ```

4. **Проверьте статус:**
   ```javascript
   rs.status()
   ```

5. **Должно показать:**
   ```json
   {
     "set": "rs0",
     "myState": 1,
     "members": [...]
   }
   ```

### Вариант 2: Использовать переменную окружения

Railway может автоматически настроить replica set, если добавить параметр в connection string:

1. **Откройте сервис plazma → Settings → Variables**
2. **Найдите DATABASE_URL** (или создайте, если используете Reference Variable)
3. **Добавьте параметр `replicaSet=rs0`** в connection string:
   ```
   mongodb://mongo:password@host:port/plazma_bot?authSource=admin&replicaSet=rs0
   ```

**Важно:** Это может не сработать, если Railway MongoDB не настроен как replica set.

### Вариант 3: Использовать Mongoose вместо Prisma

Если настройка replica set не работает, можно заменить Prisma на Mongoose, который не требует replica set.

**Преимущества Mongoose:**
- ✅ Не требует replica set
- ✅ Работает с Railway MongoDB
- ✅ Простая миграция с Prisma

**Недостатки:**
- ❌ Требует рефакторинга кода
- ❌ Потеря типизации Prisma
- ❌ Нужно переписать все модели

## 🚀 Пошаговая инструкция: Настройка replica set

### Шаг 1: Подключитесь к Railway MongoDB

```bash
# Установите Railway CLI (если еще не установлен)
npm i -g @railway/cli

# Подключитесь к проекту
railway link

# Откройте MongoDB shell
railway run mongosh
```

### Шаг 2: Инициализируйте replica set

В MongoDB shell выполните:

```javascript
// Проверьте текущий статус
rs.status()

// Если ошибка "not yet initialized", инициализируйте:
rs.initiate({
  _id: "rs0",
  members: [
    { _id: 0, host: "localhost:27017" }
  ]
})

// Подождите несколько секунд и проверьте статус
rs.status()
```

### Шаг 3: Обновите DATABASE_URL

1. **Откройте Railway Dashboard → сервис plazma → Settings → Variables**
2. **Найдите DATABASE_URL**
3. **Добавьте параметр `replicaSet=rs0`**:
   ```
   mongodb://mongo:password@host:port/plazma_bot?authSource=admin&replicaSet=rs0
   ```

### Шаг 4: Перезапустите сервис

Railway автоматически перезапустит при изменении переменных.

### Шаг 5: Проверьте логи

Должно быть:
```
Database connected
✅ Initial data ensured
```

НЕ должно быть:
- ❌ `Prisma needs to perform transactions, which requires your MongoDB server to be run as a replica set`
- ❌ `Invalid prisma.user.update() invocation`

## 🆘 Если не работает

### Проблема: "replica set not initialized"

**Решение:**
1. Убедитесь, что подключились к правильному MongoDB сервису
2. Проверьте, что используете `mongosh` (не старый `mongo`)
3. Попробуйте перезапустить MongoDB сервис на Railway

### Проблема: "replica set configuration is invalid"

**Решение:**
1. Проверьте, что используете правильный host (может быть не `localhost`)
2. Попробуйте использовать IP или hostname из connection string Railway
3. Проверьте, что MongoDB сервис запущен

### Проблема: Replica set не сохраняется после перезапуска

**Решение:**
1. Railway может сбрасывать конфигурацию при перезапуске
2. Рассмотрите использование MongoDB Atlas (надежнее)
3. Или используйте Mongoose вместо Prisma

## 🔄 Альтернатива: Использовать Mongoose

Если настройка replica set не работает, можно заменить Prisma на Mongoose:

1. **Установите Mongoose:**
   ```bash
   npm install mongoose
   ```

2. **Создайте модели Mongoose** вместо Prisma схем
3. **Обновите все сервисы** для использования Mongoose
4. **Удалите Prisma** из проекта

**Это большой рефакторинг, но Mongoose не требует replica set.**

## ✅ Рекомендация

**Для продакшена:** Используйте MongoDB Atlas - это надежнее и проще.

**Для разработки/тестирования:** Попробуйте настроить Railway MongoDB как replica set по инструкции выше.

## 📚 Дополнительная информация

- [MongoDB Replica Set Setup](https://www.mongodb.com/docs/manual/tutorial/deploy-replica-set/)
- [Railway MongoDB Documentation](https://docs.railway.app/databases/mongodb)
- [Prisma MongoDB Requirements](https://www.prisma.io/docs/concepts/database-connectors/mongodb#replica-set-requirement)
