# ⚡ Быстрая настройка переменных окружения в Railway

## 🎯 Текущая ситуация

У вас уже есть:
- ✅ MongoDB сервис запущен
- ✅ BOT_TOKEN установлен
- ✅ MONGO_PUBLIC_URL есть (но нужно использовать DATABASE_URL)

## 🚀 Шаги для исправления

### Шаг 1: Добавьте DATABASE_URL

**Способ А: Использовать Reference (Рекомендуется)**

1. В сервисе **"vital"** перейдите на вкладку **"Variables"**
2. Нажмите **"+ New Variable"**
3. Нажмите на кнопку **"Reference"** (справа от поля ввода или рядом с "+ New Variable")
4. Выберите сервис **"MongoDB"**
5. Выберите переменную **`MONGO_PUBLIC_URL`** или **`MONGO_URL`**
6. Назовите переменную **`DATABASE_URL`**
7. Сохраните

**Способ Б: Скопировать значение вручную**

1. В сервисе **"vital"** перейдите на вкладку **"Variables"**
2. Найдите переменную **`MONGO_PUBLIC_URL`**
3. Скопируйте её значение (нажмите на иконку глаза или три точки → Copy)
4. Нажмите **"+ New Variable"**
5. Добавьте:
   - **Name:** `DATABASE_URL`
   - **Value:** Вставьте скопированное значение из MONGO_PUBLIC_URL
6. Сохраните

### Шаг 2: Добавьте обязательные переменные

В сервисе **"vital"** на вкладке **"Variables"** проверьте наличие:

#### ✅ Обязательные переменные:

1. **`DATABASE_URL`** - ✅ Создать (см. Шаг 1)
2. **`BOT_TOKEN`** - ✅ Уже есть
3. **`ADMIN_EMAIL`** - ❌ Добавить
   - Нажмите **"+ New Variable"**
   - Name: `ADMIN_EMAIL`
   - Value: ваш email (например: `admin@vital.com`)

4. **`ADMIN_PASSWORD`** - ❌ Добавить
   - Нажмите **"+ New Variable"**
   - Name: `ADMIN_PASSWORD`
   - Value: ваш надежный пароль для админ-панели

#### 📋 Рекомендуемые переменные:

5. **`ADMIN_CHAT_ID`** - Рекомендуется добавить
   - Name: `ADMIN_CHAT_ID`
   - Value: ваш Telegram Chat ID
   - Как получить: откройте [@userinfobot](https://t.me/userinfobot) в Telegram, скопируйте ваш `Id`

6. **`PUBLIC_BASE_URL`** - Добавить после первого деплоя
   - Name: `PUBLIC_BASE_URL`
   - Value: `https://ваш-проект.up.railway.app` (Railway предоставит URL после деплоя)

7. **`WEBAPP_URL`** - Добавить после первого деплоя
   - Name: `WEBAPP_URL`
   - Value: `https://ваш-проект.up.railway.app/webapp`

### Шаг 3: Перезапустите сервис

1. Перейдите на вкладку **"Deployments"**
2. Найдите последний деплой
3. Нажмите **"..."** (три точки) или **"Redeploy"**
4. Подождите завершения деплоя

### Шаг 4: Проверьте логи

1. В разделе **"Deployments"** откройте последний деплой
2. Нажмите **"View Logs"**
3. Убедитесь, что видите:
   ```
   ✅ Database connected
   ✅ Initial data ensured
   ✅ Server is running on port 3000
   ✅ Bot launched successfully
   ```

## 📝 Чек-лист переменных окружения

Убедитесь, что в сервисе **"vital"** есть все эти переменные:

```
✅ DATABASE_URL      (из MONGO_PUBLIC_URL)
✅ BOT_TOKEN         (уже есть)
✅ ADMIN_EMAIL       (добавить)
✅ ADMIN_PASSWORD    (добавить)
📋 ADMIN_CHAT_ID     (рекомендуется)
📋 PUBLIC_BASE_URL   (после деплоя)
📋 WEBAPP_URL        (после деплоя)
```

## 🎯 Быстрая инструкция (TL;DR)

1. **В сервисе "vital" → Variables → + New Variable → Reference**
2. **Выберите MongoDB сервис → MONGO_PUBLIC_URL → Назовите DATABASE_URL**
3. **Добавьте ADMIN_EMAIL и ADMIN_PASSWORD**
4. **Redeploy сервиса**
5. **Проверьте логи**

---

**✅ После добавления всех переменных бот должен успешно запуститься!**









