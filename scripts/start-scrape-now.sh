#!/bin/bash

# Скрипт для запуска скрапинга изображений через админку
# Использование: ./scripts/start-scrape-now.sh

echo "🚀 Запуск скрапинга изображений через админку..."
echo ""

# Определяем URL админки из переменной окружения или используем дефолтный
ADMIN_URL="${ADMIN_URL:-http://localhost:3000}"
ENDPOINT="${ADMIN_URL}/admin/api/scrape-all-images"

echo "📡 Отправка запроса к: ${ENDPOINT}"
echo ""

# Отправляем POST запрос
RESPONSE=$(curl -s -X POST "${ENDPOINT}" \
  -H "Content-Type: application/json" \
  -w "\nHTTP_STATUS:%{http_code}")

HTTP_STATUS=$(echo "$RESPONSE" | grep "HTTP_STATUS" | cut -d: -f2)
BODY=$(echo "$RESPONSE" | sed '/HTTP_STATUS/d')

if [ "$HTTP_STATUS" = "200" ] || [ "$HTTP_STATUS" = "401" ]; then
  echo "✅ Запрос отправлен успешно!"
  echo ""
  echo "📝 Ответ сервера:"
  echo "$BODY" | jq '.' 2>/dev/null || echo "$BODY"
  echo ""
  echo "💡 Проверьте логи сервера для отслеживания прогресса."
  echo "   Скрапинг запущен в фоновом режиме."
else
  echo "❌ Ошибка: HTTP ${HTTP_STATUS}"
  echo ""
  echo "Ответ:"
  echo "$BODY"
  echo ""
  echo "💡 Возможно, нужно сначала авторизоваться в админке."
fi



