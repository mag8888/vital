#!/usr/bin/env node

/**
 * Скрипт для обновления текста "О проекте" в базе данных
 * Убирает "Научно доказанные преимущества" и добавляет предупреждение
 */

import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();

const newAboutText = `🌟 Plazma Water - это революционная технология структурирования воды!

💧 Наша вода проходит специальную обработку, которая:
• Улучшает структуру молекул воды
• Повышает биологическую активность
• Способствует лучшему усвоению организмом

• Усиление иммунитета
• Улучшение обмена веществ
• Повышение энергии и жизненного тонуса
• Антиоксидантные свойства

🌱 Экологически чистая технология без химических добавок!

⚠️ Не является лекарственным средством.`;

async function updateAboutText() {
  try {
    console.log('🔄 Обновление текста "О проекте"...');
    
    // Обновляем или создаем запись
    const updated = await prisma.botContent.upsert({
      where: {
        key: 'about_text'
      },
      update: {
        content: newAboutText
      },
      create: {
        key: 'about_text',
        title: 'О проекте',
        content: newAboutText,
        description: 'Текст о проекте Plazma Water',
        category: 'descriptions',
        language: 'ru',
        isActive: true
      }
    });
    
    console.log('✅ Текст "О проекте" успешно обновлен!');
    console.log('📝 ID записи:', updated.id);
    
  } catch (error) {
    console.error('❌ Ошибка обновления текста:', error);
    process.exit(1);
  } finally {
    await prisma.$disconnect();
  }
}

updateAboutText();

