# Настройка видео в мини-апп

## Описание
Добавлен функционал для показа видео в разделе "О PLASMA" в мини-апп.

## Что было реализовано:

### 1. API endpoint для получения ссылки на видео
- **URL**: `GET /api/video/url`
- **Ответ**: `{ "videoUrl": "https://..." }`
- **Настройка**: Через переменную окружения `VIDEO_URL`

### 2. Улучшенная функция показа видео
- Получение ссылки на видео с сервера
- Поддержка Telegram WebApp API
- Fallback для обычных браузеров
- Обработка ошибок

### 3. Настройка через переменные окружения
- Добавлена переменная `VIDEO_URL` в конфигурацию
- Значение по умолчанию: `https://www.youtube.com/watch?v=dQw4w9WgXcQ`

## Настройка:

### 1. Установка ссылки на видео
Добавьте в ваш `.env` файл:
```env
VIDEO_URL=https://www.youtube.com/watch?v=YOUR_VIDEO_ID
```

### 2. Поддерживаемые форматы ссылок:
- YouTube: `https://www.youtube.com/watch?v=VIDEO_ID`
- Vimeo: `https://vimeo.com/VIDEO_ID`
- Прямые ссылки на видео: `https://example.com/video.mp4`

### 3. Настройка Telegram канала
В файле `webapp/app.js` найдите функцию `openTelegram()` и замените:
```javascript
const telegramUrl = 'https://t.me/your_channel_username';
```
на реальную ссылку на ваш канал.

## Логика работы:

1. **Пользователь нажимает "Смотреть видео"**
2. **Мини-апп запрашивает ссылку с сервера** (`GET /api/video/url`)
3. **Сервер возвращает ссылку из переменной окружения**
4. **Видео открывается:**
   - В Telegram (если доступен `tg.openLink`)
   - В новом окне/вкладке (fallback)

## Логи для отладки:

### В консоли браузера:
```
🎥 Getting video URL...
✅ Video URL received: https://www.youtube.com/watch?v=...
```

### На сервере:
```
GET /api/video/url - получение ссылки на видео
```

## Обработка ошибок:

- **Ошибка получения ссылки**: Показывается "Ошибка получения ссылки на видео"
- **Ошибка открытия**: Показывается "Ошибка открытия видео"
- **Fallback**: Если Telegram API недоступен, открывается в браузере

## Тестирование:

1. Установите переменную `VIDEO_URL` в `.env`
2. Перезапустите сервер
3. Откройте мини-апп
4. Перейдите в раздел "О PLASMA"
5. Нажмите "Смотреть видео"
6. Проверьте, что видео открывается

## Примеры ссылок:

```env
# YouTube видео
VIDEO_URL=https://www.youtube.com/watch?v=dQw4w9WgXcQ

# Vimeo видео
VIDEO_URL=https://vimeo.com/123456789

# Прямая ссылка на видео
VIDEO_URL=https://example.com/video.mp4
```

## Дополнительные возможности:

### Telegram канал
Также можно настроить ссылку на Telegram канал в функции `openTelegram()`:
```javascript
const telegramUrl = 'https://t.me/plazma_water_channel';
```

### Социальные сети
Можно добавить аналогичную функциональность для других социальных сетей, создав соответствующие API endpoints и функции.
