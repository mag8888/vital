# 🚂 Подключение бота к Railway

## Шаг 1: Подготовка репозитория

### 1.1. Убедитесь, что все файлы готовы
- ✅ `nixpacks.toml` - конфигурация для Railway
- ✅ `.railwayignore` - файлы, которые не нужно отправлять на Railway
- ✅ `package.json` с правильными скриптами

### 1.2. Закоммитьте изменения
```bash
git add .
git commit -m "Add Railway configuration"
git push origin main
```

## Шаг 2: Создание проекта на Railway

### 2.1. Войдите на Railway
1. Перейдите на [railway.app](https://railway.app)
2. Войдите через GitHub аккаунт

### 2.2. Создайте новый проект
1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите ваш репозиторий с ботом
4. Railway автоматически определит Node.js проект

### 2.3. Добавьте базу данных MongoDB
1. В проекте нажмите **"+ New"**
2. Выберите **"Database"** → **"MongoDB"**
3. Railway автоматически создаст MongoDB инстанс

**ВАЖНО:** После создания MongoDB:
1. Откройте созданный MongoDB сервис
2. Перейдите на вкладку **"Variables"**
3. Найдите переменную `MONGO_URL` или `MONGODB_URL`
4. Вернитесь в основной сервис (где запускается бот)
5. Перейдите на вкладку **"Variables"**
6. Нажмите **"+ New Variable"**
7. Нажмите на **"Reference"** (справа от поля ввода)
8. Выберите ваш MongoDB сервис
9. Выберите переменную `MONGO_URL` или `MONGODB_URL`
10. Назовите переменную `DATABASE_URL` (или используйте автоматическое имя)
11. Railway автоматически создаст ссылку на базу данных

## Шаг 3: Настройка переменных окружения

### 3.1. Откройте настройки проекта
1. В проекте нажмите на **"Variables"** вкладку
2. Или перейдите в **Settings** → **Variables**

### 3.2. Добавьте обязательные переменные

#### Бот Telegram:
```env
BOT_TOKEN=your_bot_token_from_botfather
```

Получить токен:
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` или выберите существующего бота
3. Скопируйте токен и вставьте в `BOT_TOKEN`

#### Админ-панель:
```env
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=your_secure_password_here
ADMIN_CHAT_ID=your_telegram_chat_id
```

Получить `ADMIN_CHAT_ID`:
1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Откройте бота
3. Скопируйте ваш `Id` (число)
4. Если нужно несколько админов, укажите через запятую: `123456,789012`

#### URL приложения:
После первого деплоя Railway автоматически присвоит URL вида:
`https://your-project-name.up.railway.app`

Добавьте переменные (замените на ваш реальный URL):
```env
PUBLIC_BASE_URL=https://your-project-name.up.railway.app
WEBAPP_URL=https://your-project-name.up.railway.app/webapp
```

#### Опциональные переменные:
```env
BOT_WEBHOOK_URL=https://your-project-name.up.railway.app/webhook/bot
BOT_WEBHOOK_SECRET=your_random_secret_string
SESSION_SECRET=your_random_session_secret
VIDEO_URL=https://www.youtube.com/watch?v=your_video_id
```

**⚠️ ВАЖНО:** `DATABASE_URL` НЕ добавляется автоматически при создании MongoDB! 

Вам нужно вручную добавить ссылку на MongoDB:
1. В основном сервисе перейдите на вкладку **"Variables"**
2. Нажмите **"+ New Variable"** → **"Reference"**
3. Выберите ваш MongoDB сервис и переменную `MONGO_URL` или `MONGODB_URL`
4. Назовите её `DATABASE_URL`

**Или** скопируйте значение `MONGO_URL` из MongoDB сервиса и вставьте в новую переменную `DATABASE_URL` в основном сервисе.

**Если ошибка продолжается** - смотрите подробную инструкцию в `RAILWAY_DATABASE_FIX.md`

## Шаг 4: Первый деплой

### 4.1. Автоматический деплой
Railway автоматически начнет деплой при подключении репозитория.

### 4.2. Проверка процесса деплоя
1. Перейдите в раздел **"Deployments"**
2. Дождитесь завершения сборки (зеленый статус)
3. Проверьте логи на наличие ошибок

### 4.3. Обновите URL переменные
После первого успешного деплоя:
1. Railway присвоит домен (например: `https://your-project.up.railway.app`)
2. Обновите переменные окружения:
   - `PUBLIC_BASE_URL`
   - `WEBAPP_URL`
3. Нажмите **"Redeploy"** в разделе Deployments

## Шаг 5: Настройка базы данных

### 5.1. Применить миграции Prisma
После первого деплоя выполните:

Через Railway CLI:
```bash
# Установите Railway CLI
npm install -g @railway/cli

# Войдите в аккаунт
railway login

# Подключитесь к проекту
railway link

# Примените миграции
railway run npx prisma db push
```

Или через Railway Dashboard:
1. Перейдите в ваш проект
2. Откройте вкладку **"Deployments"**
3. Нажмите **"..."** на последнем деплое
4. Выберите **"Open Shell"**
5. Выполните: `npx prisma db push`

## Шаг 6: Настройка веб-приложения в боте

### 6.1. Автоматическая настройка
После успешного деплоя выполните:

Через Railway CLI:
```bash
railway run npm run setup-railway
```

Или через Railway Shell (Deployments → ... → Open Shell):
```bash
npm run setup-railway
```

### 6.2. Ручная настройка через BotFather
1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите **"Menu Button"**
5. Введите URL: `https://your-project.up.railway.app/webapp`
6. Введите текст кнопки: `🌐 Веб-приложение`

## Шаг 7: Проверка работоспособности

### 7.1. Health Check
Откройте в браузере:
```
https://your-project.up.railway.app/health
```

Должен вернуться JSON:
```json
{
  "status": "ok",
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

### 7.2. Проверка веб-приложения
```
https://your-project.up.railway.app/webapp
```

Должна загрузиться главная страница веб-приложения.

### 7.3. Проверка бота
1. Откройте вашего бота в Telegram
2. Отправьте `/start`
3. Проверьте, что бот отвечает
4. Проверьте наличие кнопки "🌐 Веб-приложение" в меню

### 7.4. Проверка админ-панели
```
https://your-project.up.railway.app/admin
```

Должна открыться страница входа в админ-панель.

## Шаг 8: Мониторинг и логи

### 8.1. Просмотр логов
1. В Railway Dashboard перейдите в раздел **"Deployments"**
2. Выберите последний деплой
3. Нажмите **"View Logs"**

### 8.2. Метрики
Railway автоматически предоставляет:
- Использование CPU
- Использование памяти
- Сетевой трафик
- Время ответа

### 8.3. Автоматические рестарты
Railway автоматически перезапустит приложение при:
- Ошибках в коде
- Превышении лимитов памяти
- Изменении переменных окружения

## Шаг 9: Обновление приложения

### 9.1. Автоматическое обновление
Railway автоматически развернет изменения при пуше в основную ветку:

```bash
git add .
git commit -m "Update bot features"
git push origin main
```

### 9.2. Ручной редеплой
В Railway Dashboard:
1. Перейдите в раздел **"Deployments"**
2. Нажмите **"Redeploy"** на последнем деплое

## Решение проблем

### Проблема: Сборка не проходит
**Решение:**
1. Проверьте логи в Railway Dashboard
2. Убедитесь, что все зависимости в `package.json`
3. Проверьте, что `prisma generate` выполняется перед `build`

### Проблема: База данных не подключена
**Решение:**
1. Проверьте, что `DATABASE_URL` правильно установлена
2. Убедитесь, что MongoDB инстанс создан
3. Проверьте подключение через логи

### Проблема: Бот не отвечает
**Решение:**
1. Проверьте, что `BOT_TOKEN` правильный
2. Убедитесь, что бот запущен (проверьте логи)
3. Проверьте, что нет ошибок в логах

### Проблема: Веб-приложение не открывается
**Решение:**
1. Проверьте, что `PUBLIC_BASE_URL` и `WEBAPP_URL` установлены правильно
2. Убедитесь, что сервер запущен
3. Проверьте логи на наличие ошибок

## Полезные ссылки

- [Railway Documentation](https://docs.railway.app/)
- [Railway Dashboard](https://railway.app/dashboard)
- [Prisma Documentation](https://www.prisma.io/docs)
- [Telegraf Documentation](https://telegraf.js.org/)

## Чек-лист перед запуском

- [ ] Репозиторий подключен к Railway
- [ ] MongoDB база данных создана
- [ ] Все переменные окружения установлены
- [ ] Первый деплой успешно завершен
- [ ] Миграции Prisma применены
- [ ] Health check возвращает `ok`
- [ ] Бот отвечает в Telegram
- [ ] Веб-приложение открывается
- [ ] Админ-панель доступна

---

**🎉 Готово! Ваш бот теперь работает на Railway!**

