# 🚀 Запуск скрапинга и проверка изображений

## Шаг 1: Запуск скрапинга изображений

### Через админку (самый простой способ):
1. Откройте админку: `https://ваш-домен/admin/products`
2. Нажмите кнопку **"📸 Собрать ВСЕ фото с сайта"**
3. Процесс запустится автоматически в фоне
4. Проверьте логи сервера для отслеживания прогресса

### Через Railway CLI:
```bash
railway login
railway run npm run scrape-all-images-now
```

### Через Railway Web Interface:
1. Зайдите в проект на Railway
2. Откройте вкладку **"Deployments"**
3. Нажмите на **три точки** рядом с последним деплоем
4. Выберите **"Run Command"**
5. Введите: `npm run scrape-all-images-now`
6. Нажмите **"Run"**

## Шаг 2: Проверка результатов

После завершения скрапинга проверьте результаты:

```bash
npm run verify-images
```

Или через Railway:
```bash
railway run npm run verify-images
```

## Что проверяется:

✅ **Товары с изображениями** - сколько товаров имеют изображения  
❌ **Товары без изображений** - список товаров, которые нужно обработать  
🔍 **Доступность изображений** - проверка, что URL изображений работают  

## Ожидаемый результат:

- ✅ Все активные товары должны иметь изображения
- ✅ Все URL изображений должны быть доступны
- ✅ Изображения должны быть высокого качества

## Если есть проблемы:

1. **Товары не найдены в БД** - убедитесь, что товары импортированы
2. **Изображения не загружаются** - проверьте настройки Cloudinary (если используется)
3. **URL недоступны** - возможно, сайт Siam Botanicals временно недоступен

## Логи процесса:

Все логи процесса сохраняются в консоли сервера. Вы увидите:
- 📦 Какие товары обрабатываются
- ✅ Успешно загруженные изображения
- ❌ Ошибки при загрузке
- 📊 Финальная статистика

