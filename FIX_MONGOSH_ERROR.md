# 🔧 Исправление ошибки "No such file or directory" для mongosh

## Проблема

При выполнении `railway run mongosh` появляется ошибка:
```
No such file or directory (os error 2)
```

## ✅ Решение

### Вариант 1: Подключитесь к MongoDB сервису напрямую

Нужно выполнить команду в контексте MongoDB сервиса, а не основного сервиса.

1. **Отключитесь от текущего сервиса:**
   ```bash
   railway unlink
   ```

2. **Подключитесь к MongoDB сервису:**
   ```bash
   railway link
   ```
   - Выберите workspace: `mag8888's Projects`
   - Выберите проект: `PLAZMA`
   - Выберите environment: `production`
   - **Выберите сервис: `MongoDB`** (не `plazma`!)

3. **Откройте MongoDB shell:**
   ```bash
   railway run mongosh
   ```

### Вариант 2: Используйте полный путь к mongosh

Попробуйте:

```bash
railway run /usr/bin/mongosh
```

или

```bash
railway run mongo
```

### Вариант 3: Используйте переменную окружения MONGO_URL

Если mongosh недоступен, можно использовать connection string напрямую:

1. **Получите MONGO_URL:**
   ```bash
   railway variables
   ```
   Найдите `MONGO_URL` или `DATABASE_URL`

2. **Используйте локальный mongosh:**
   ```bash
   mongosh "mongodb://mongo:password@host:port/plazma_bot?authSource=admin"
   ```

### Вариант 4: Используйте Railway Dashboard

1. Откройте Railway Dashboard
2. Сервис **MongoDB** → **Database** → **Connect**
3. Используйте Connection String для подключения через локальный `mongosh`

## 🎯 Рекомендуемый способ

**Вариант 1** - подключиться к MongoDB сервису напрямую через `railway link` и выбрать сервис `MongoDB`.

## 📝 После успешного подключения

В MongoDB shell выполните:

```javascript
rs.initiate({
  _id: "rs0",
  members: [
    { _id: 0, host: "localhost:27017" }
  ]
})

// Подождите 5-10 секунд
rs.status()
```
