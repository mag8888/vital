# 🔐 Сброс пароля MongoDB для исправления аутентификации

## Проблема

Ошибка `SCRAM failure: Authentication failed` означает, что используется старый/неправильный пароль MongoDB.

## Решение: Сбросить пароль MongoDB

### Шаг 1: Сброс пароля

1. **Откройте Railway Dashboard**
2. **Откройте сервис `MongoDB`** (не `vital`!)
3. Перейдите во вкладку **Settings**
4. Найдите секцию **"Reset Password"** или **"Security"**
5. Нажмите **"Reset Password"** или **"Generate New Password"**
6. Railway сгенерирует новый пароль
7. **Важно:** `MONGO_PUBLIC_URL` автоматически обновится с новым паролем

### Шаг 2: Проверьте переменную в сервисе `vital`

1. **Откройте сервис `vital`**
2. Перейдите в **Variables**
3. Найдите переменную `DATABASE_URL`
4. Убедитесь, что она использует **Reference** (ссылку):
   - Должно быть: `{{MongoDB.MONGO_PUBLIC_URL}}`
   - Или зелёная иконка ссылки рядом с переменной

### Шаг 3: Перезапуск

После сброса пароля:

1. Railway автоматически перезапустит сервис `vital` (так как используется ссылка)
2. Или вручную: **Deployments** → **Redeploy**

### Шаг 4: Проверка логов

После перезапуска проверьте логи:

1. **Deployments** → последний деплой → **View Logs**
2. Должно быть:
   - ✅ `Database connected`
   - ✅ `Bot launched successfully`
   - ❌ НЕ должно быть: `Authentication failed` или `SCRAM failure`

## Альтернатива: Если сброс пароля не помог

Если после сброса пароля проблема осталась:

1. **Удалите переменную `DATABASE_URL`** в сервисе `vital`
2. **Создайте заново** через Reference:
   - **Add Reference** → выберите `MongoDB` → `MONGO_PUBLIC_URL`
3. Сохраните и перезапустите

## Важно!

- ✅ **Используйте Reference**, а не копируйте URL
- ✅ После сброса пароля ссылка автоматически обновится
- ✅ Не нужно вручную обновлять URL после сброса пароля










