# ⚡ Быстрое переподключение MongoDB

## Шаги:

1. **Откройте сервис `vital`** (не MongoDB!)

2. **Variables** → найдите или создайте `DATABASE_URL`

3. **Важно!** Используйте **Reference** (ссылку), а не копируйте URL:
   - Нажмите на иконку **🔗 Reference** рядом с полем Value
   - Или введите напрямую: `{{MongoDB.MONGO_PUBLIC_URL}}`

4. **Сохраните** → Railway автоматически перезапустит сервис

5. **Проверьте логи** → должно быть ✅ `Database connected`

## Если не работает:

- Убедитесь, что используется ссылка `{{MongoDB.MONGO_PUBLIC_URL}}`, а не скопированный URL
- MongoDB сервис должен быть зелёным (запущен)
- Если нужно — сбросьте пароль MongoDB: MongoDB → Settings → Reset Password











