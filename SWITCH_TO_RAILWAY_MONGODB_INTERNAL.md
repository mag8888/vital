# 🔄 Переключение на внутреннюю MongoDB Railway

## 📋 Текущая ситуация

Сейчас используются внешние базы данных (MongoDB Atlas), что вызывает ошибки аутентификации:
```
ConnectorError: SCRAM failure: Authentication failed
```

## ✅ Решение: Использовать Railway MongoDB

Railway предоставляет встроенный MongoDB, который работает в той же инфраструктуре и не требует внешних подключений.

## 🚀 Пошаговая инструкция

### Шаг 1: Проверьте наличие MongoDB на Railway

1. Откройте [Railway Dashboard](https://railway.app)
2. Выберите проект `plazma-production`
3. Проверьте, есть ли сервис **MongoDB** в списке сервисов

**Если MongoDB нет:**
- Нажмите **"+ New"** → **"Database"** → **"Add MongoDB"**
- Дождитесь создания (1-2 минуты)
- Railway автоматически создаст переменную `MONGO_URL`

### Шаг 2: Настройте DATABASE_URL в основном сервисе

1. Откройте сервис **plazma** (основной сервис бота)
2. Перейдите в **Settings** → **Variables**
3. Найдите переменную `DATABASE_URL`

**Вариант A: Использовать Reference Variable (Рекомендуется)**
- Нажмите **"Add Reference"** или **"New Variable"**
- **Key:** `DATABASE_URL`
- **Value:** `${{MongoDB.MONGO_URL}}`
- Это создаст Reference Variable, которая автоматически синхронизируется

**Вариант B: Скопировать значение вручную**
- Откройте сервис **MongoDB** → **Variables**
- Скопируйте значение `MONGO_URL`
- Вставьте в `DATABASE_URL` основного сервиса
- Убедитесь, что добавлено имя базы: `/plazma_bot` или `/plazma`

### Шаг 3: Проверьте формат connection string

Railway MongoDB connection string должен выглядеть так:
```
mongodb://mongo:password@host:port/plazma_bot?authSource=admin
```

**Важно:**
- Должно быть имя базы данных: `/plazma_bot` или `/plazma`
- Должен быть параметр `authSource=admin`
- Код автоматически добавит эти параметры, если их нет

### Шаг 4: Перезапустите сервис

1. Railway автоматически перезапустит сервис при изменении переменных
2. Или вручную: **Deployments** → **Redeploy**

### Шаг 5: Проверьте логи

После перезапуска в логах должно появиться:
```
✅ Railway MongoDB detected
Database URL configured: mongodb://mongo:...
Added default database name: plazma_bot
Added authSource=admin for Railway MongoDB
Database connected
```

**Если видите ошибки:**
- Проверьте формат `DATABASE_URL`
- Убедитесь, что используется Reference Variable `${{MongoDB.MONGO_URL}}`
- Проверьте, что MongoDB сервис запущен (статус "Online")

## 🔄 Восстановление данных (если нужно)

Если у вас есть данные в старой базе:

1. **Экспорт из старой базы:**
```bash
# Если есть доступ к старой базе
mongodump --uri="OLD_DATABASE_URL" --out=./backup
```

2. **Импорт в Railway MongoDB:**
```bash
# Через Railway CLI
railway link
railway run mongorestore --uri="$MONGO_URL" ./backup/plazma_bot
```

Или используйте скрипт восстановления из Cloudinary:
```bash
railway run npm run restore
```

## ✅ Проверка работы

После настройки проверьте:

1. **Логи сервера:**
   - Должно быть: `Database connected` (без ошибок)
   - Не должно быть: `SCRAM failure` или `Authentication failed`

2. **Работа бота:**
   - Откройте магазин - должно работать
   - Откройте отзывы - должно работать
   - Откройте партнерку - должно работать

3. **База данных:**
   - В Railway MongoDB → **Data** можно увидеть коллекции
   - Должны быть коллекции: `User`, `Product`, `Category`, `Review` и т.д.

## 🆘 Решение проблем

### Проблема: Все еще ошибки аутентификации

**Решение:**
1. Удалите старую переменную `DATABASE_URL`
2. Создайте новую с Reference Variable: `${{MongoDB.MONGO_URL}}`
3. Перезапустите сервис

### Проблема: База данных пустая

**Решение:**
1. Восстановите данные из бэкапа: `railway run npm run restore`
2. Или примените начальные данные через Prisma: `railway run npx prisma db push`

### Проблема: Не видно MongoDB сервиса

**Решение:**
1. Создайте новый MongoDB: **"+ New"** → **"Database"** → **"Add MongoDB"**
2. Дождитесь полного создания (статус "Online")

## 📚 Дополнительная информация

- [Railway MongoDB Documentation](https://docs.railway.app/databases/mongodb)
- [Prisma MongoDB Setup](https://www.prisma.io/docs/concepts/database-connectors/mongodb)
