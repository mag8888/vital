# 📸 Сбор недостающих фотографий продуктов

## Описание

Скрипт автоматически собирает все недостающие фотографии продуктов с сайта Siam Botanicals и добавляет их к товарам в базе данных.

## Запуск на Railway (рекомендуется)

Скрипт должен запускаться на Railway, где уже настроены все переменные окружения:

```bash
railway run npm run scrape-all-images
```

Или через Railway CLI:

```bash
railway run npx tsx scripts/scrape-all-missing-images.ts
```

## Что делает скрипт

1. **Парсит страницы магазина** Siam Botanicals (https://siambotanicals.com/shop/)
   - Проходит по всем страницам (до 20 страниц)
   - Извлекает информацию о всех продуктах

2. **Находит недостающие фотографии**
   - Сопоставляет продукты с базой данных (нечеткое совпадение по названию)
   - Проверяет, у каких продуктов нет изображений

3. **Собирает изображения**
   - Сначала пытается взять из списка продуктов на странице магазина
   - Если не найдено, переходит на страницу продукта и извлекает оттуда

4. **Загружает изображения**
   - Если Cloudinary настроен: загружает на Cloudinary и сохраняет URL
   - Если Cloudinary не настроен: сохраняет прямой URL изображения

5. **Обновляет базу данных**
   - Обновляет поле `imageUrl` для всех найденных продуктов

## Требования

- `DATABASE_URL` - строка подключения к MongoDB
- `CLOUDINARY_*` (опционально) - для загрузки на Cloudinary

## Результат

Скрипт выводит статистику:
- ✅ Обновлено: количество продуктов с добавленными изображениями
- ⏭️ Пропущено: продукты, у которых уже есть изображения
- ❌ Не удалось: продукты, для которых не удалось получить изображение
- 🔍 Не найдено в БД: продукты с сайта, которых нет в базе данных

## Примечания

- Скрипт делает паузы между запросами, чтобы не перегружать сайт
- Ограничение: максимум 20 страниц (можно увеличить в коде)
- Находит продукты в БД по нечеткому совпадению названий

