# 🔍 Поиск категории "Артефакт" и товаров в ней

## 📋 Результаты поиска

### ❌ Категория "Артефакт" не найдена в локальных бэкапах

Проверены следующие бэкапы:
- `database-backup-2025-12-07T17-30-20.json`
- `database-backup-2025-12-01T19-33-19.json`

### 📂 Найденные категории в бэкапах:

1. **Плазменный набор** (slug: `-`)
   - Товаров: 3
   - Товары: Плазменный набор, uyguyg (2 шт, неактивные)

2. **Тестовая категория** (slug: `test-category`)
   - Товаров: 0

3. **На каждый день** (slug: `na-kazhdyy-den`)
   - Товаров: 9
   - Товары: Противовирусная плазма, Магниевая плазма, Углеродная плазма, Цинковая плазма, Медная плазма, Железная плазма, Капли для глаз, Brain Booster Water, Серебряная плазма

## 🔍 Где искать категорию "Артефакт"

### 1. В бэкапах Cloudinary

Категория "Артефакт" может быть в более старых бэкапах, которые хранятся в Cloudinary:

```bash
# Установите учетные данные Cloudinary
export CLOUDINARY_CLOUD_NAME=dt4r1tigf
export CLOUDINARY_API_KEY=your_api_key
export CLOUDINARY_API_SECRET=your_api_secret

# Просмотр всех бэкапов
node scripts/list-cloudinary-backups.js

# Затем проверьте каждый бэкап
node scripts/find-artifact-category.js
```

### 2. В текущей базе данных

Если категория "Артефакт" существует в текущей базе данных:

```bash
# Убедитесь, что DATABASE_URL установлен
export DATABASE_URL="your_mongodb_connection_string"

# Поиск категории
node scripts/find-artifact-category.js
```

### 3. Через веб-интерфейс Cloudinary

1. Откройте https://console.cloudinary.com/
2. Перейдите в Media Library
3. Найдите папку `plazma-bot/backups`
4. Скачайте более старые бэкапы и проверьте их

## 🛠️ Полезные скрипты

### Поиск категории "Артефакт":
```bash
node scripts/find-artifact-category.js
```

### Поиск всех товаров и категорий:
```bash
node scripts/find-backup-with-artifacts.js
```

### Восстановление из бэкапа:
```bash
# Из локального файла
node scripts/restore-from-cloudinary.js "/Users/ADMIN/PLAZMA/database-backup-2025-12-01T19-33-19.json"

# Из Cloudinary (последний бэкап)
node scripts/restore-from-cloudinary.js
```

## 💡 Возможные причины

1. **Категория была удалена** - возможно, она существовала раньше, но была удалена до создания текущих бэкапов
2. **Категория в более старых бэкапах** - нужно проверить бэкапы в Cloudinary
3. **Категория называется по-другому** - проверьте все категории в бэкапах
4. **Категория еще не создана** - возможно, её нужно создать заново

## 📝 Создание категории "Артефакт"

Если категория не найдена, вы можете создать её через админ-панель или напрямую в базе данных:

1. **Через админ-панель:**
   - Откройте `/admin`
   - Перейдите в раздел "Категории"
   - Создайте новую категорию "Артефакт"

2. **Через базу данных:**
   ```javascript
   // Используйте Mongoose или MongoDB shell
   db.categories.insertOne({
     name: "Артефакт",
     slug: "artifact",
     description: "Категория артефактов",
     isActive: true,
     createdAt: new Date(),
     updatedAt: new Date()
   });
   ```

## ✅ Следующие шаги

1. Проверьте бэкапы в Cloudinary на наличие категории "Артефакт"
2. Если категория найдена в старом бэкапе, восстановите его
3. Если категория не найдена, создайте её заново
4. Добавьте товары в категорию "Артефакт"
