# 🔧 Устранение проблем с Lava.top API

## Проблема: Все endpoint'ы возвращают 404

Если все попытки создания инвойса возвращают 404, проверьте следующее:

### 1. Проверьте правильный endpoint в кабинете Lava

1. Войдите в [кабинет Lava.top](https://app.lava.top)
2. Перейдите в раздел **"Public API"** или **"API keys"**
3. Найдите документацию по API или примеры запросов
4. Проверьте точный endpoint для создания инвойса

### 2. Возможные варианты endpoint'ов

Попробуйте установить `LAVA_BASE_URL` в Railway на один из вариантов:

```env
# Вариант 1: Без /api
LAVA_BASE_URL=https://api.lava.top

# Вариант 2: С /api
LAVA_BASE_URL=https://api.lava.top/api

# Вариант 3: С /api/v2
LAVA_BASE_URL=https://api.lava.top/api/v2

# Вариант 4: Другой домен (если есть)
LAVA_BASE_URL=https://lava.top/api
```

### 3. Проверьте формат запроса

Возможно, Lava требует другой формат данных. Проверьте в документации:

- Правильные имена полей (может быть `amount` вместо `sum`)
- Обязательные поля
- Формат подписи (может быть другой алгоритм)

### 4. Использование виджета вместо API

Если API не работает, возможно, нужно использовать виджет Lava:

1. В кабинете Lava перейдите в раздел **"Web Widget"**
2. Настройте виджет для вашего проекта
3. Используйте виджет для создания платежей

### 5. Проверка авторизации

Убедитесь, что:
- `LAVA_PROJECT_ID` правильный (2304615369)
- `LAVA_SECRET_KEY` полный и правильный (не обрезанный)
- Формат заголовка `Authorization` правильный

### 6. Обращение в поддержку Lava

Если ничего не помогает:
1. Обратитесь в поддержку Lava.top
2. Укажите, что все endpoint'ы возвращают 404
3. Попросите предоставить правильный endpoint и формат запроса

### 7. Альтернативное решение

Если Lava API не работает, можно:
1. Использовать другой платежный сервис
2. Использовать виджет Lava вместо прямого API
3. Реализовать ручное создание платежей через админку

## Текущая конфигурация

Проверьте в Railway, что установлены:

```env
LAVA_PROJECT_ID=2304615369
LAVA_SECRET_KEY=zhwuUbNCgEhmBYHvzSIF0jPbFH1sVkb6iRsp8gMA0uhqXvTWqBUFdqJagkvpLw6Z
LAVA_BASE_URL=https://api.lava.top/api/v2
LAVA_WEBHOOK_SECRET=627e791c317c818b579c341f935953a83d7b5f42c2e9026a357ab0b4f790b762
PUBLIC_BASE_URL=https://vital-production.up.railway.app
```

## Логи для диагностики

При попытке создания инвойса в логах должно быть видно:
- Какие endpoint'ы пробуются
- Какой URL используется
- Какие заголовки отправляются
- Какой ответ приходит от Lava

Если все endpoint'ы возвращают 404, значит либо:
1. Неправильный base URL
2. Lava использует другой формат API
3. Нужно использовать виджет вместо API



