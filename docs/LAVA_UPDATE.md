# 🔄 Обновление интеграции Lava API

## ⚠️ ВАЖНО: Обновление переменных окружения

После обновления кода необходимо обновить переменные окружения в Railway:

### 1. Обновите LAVA_BASE_URL

**Было:**
```env
LAVA_BASE_URL=https://api.lava.top/api/v2
```

**Должно быть:**
```env
LAVA_BASE_URL=https://gate.lava.top
```

### 2. Проверьте LAVA_SECRET_KEY

Убедитесь, что `LAVA_SECRET_KEY` содержит ваш API ключ от Lava (это тот же ключ, который используется в заголовке `X-Api-Key`).

**Важно:** Теперь используется только `LAVA_SECRET_KEY` (или `LAVA_API_KEY`), больше не нужен `LAVA_PROJECT_ID`.

### 3. Полный список переменных окружения

```env
# Lava API
LAVA_BASE_URL=https://gate.lava.top
LAVA_SECRET_KEY=ваш_api_ключ_от_lava
LAVA_WEBHOOK_SECRET=ваш_секретный_ключ_для_webhook

# Общие
PUBLIC_BASE_URL=https://plazma-production.up.railway.app
```

## 📋 Что изменилось

### Формат API запросов

**Было (старый формат):**
- URL: `https://api.lava.top/api/v2/invoice/create`
- Заголовки: `Authorization: Bearer`, `X-Project-Id`, `X-Signature`, `X-Timestamp`
- Параметры: `sum`, `orderId`, `hookUrl`, `successUrl`, `failUrl`

**Стало (новый формат согласно документации):**
- URL: `https://gate.lava.top/api/v2/invoice`
- Заголовки: `X-Api-Key: ваш_api_ключ`
- Параметры: `email`, `sum`, `orderId`, `currency`, `buyerLanguage`, `hookUrl`, `successUrl`, `failUrl`

### Обязательные параметры

Теперь при создании инвойса обязательно нужно передавать:
- `email` - email покупателя (генерируется автоматически, если у пользователя нет email)
- `currency` - валюта (`RUB`, `USD`, `EUR`)
- `buyerLanguage` - язык покупателя (`RU`, `EN`)

## 🔍 Проверка после обновления

1. Обновите переменные окружения в Railway
2. Перезапустите приложение
3. Попробуйте команду `/add_balance` в боте
4. Проверьте логи - должно быть видно успешный запрос к `https://gate.lava.top/api/v2/invoice`

## 📚 Документация

Официальная документация Lava API: https://gate.lava.top/docs

