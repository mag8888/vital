# Техническое задание: Админ-панель Vital Bot

## 📋 Оглавление
1. [Общее описание](#общее-описание)
2. [Архитектура системы](#архитектура-системы)
3. [Функциональные модули](#функциональные-модули)
4. [Интерфейс и UX](#интерфейс-и-ux)
5. [Безопасность](#безопасность)
6. [API Endpoints](#api-endpoints)
7. [База данных](#база-данных)
8. [Возможные улучшения](#возможные-улучшения)

---

## 🎯 Общее описание

### Назначение
Веб-панель для управления Telegram-ботом Plazma, включающая управление пользователями, товарами, партнерской программой, заказами и аналитикой.

### Технологический стек
- **Backend**: Node.js + Express.js + TypeScript
- **Database**: MongoDB + Prisma ORM
- **Frontend**: Server-side rendering (SSR) с встроенным JavaScript
- **Auth**: Express Session
- **File Upload**: Multer + Cloudinary
- **Notifications**: Telegram Bot API

### Целевая аудитория
- Администраторы бота
- Менеджеры по работе с клиентами
- Аналитики

---

## 🏗 Архитектура системы

### Основные компоненты

```
┌─────────────────────────────────────────────────┐
│                 Admin Panel                      │
├─────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │  Users   │  │ Partners │  │ Products │      │
│  │Management│  │ Program  │  │ Catalog  │      │
│  └──────────┘  └──────────┘  └──────────┘      │
│                                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │  Orders  │  │ Analytics│  │ Messages │      │
│  │Management│  │ & Stats  │  │ Sender   │      │
│  └──────────┘  └──────────┘  └──────────┘      │
└─────────────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────┐
│            Express.js Router Layer               │
│  • Authentication Middleware                     │
│  • Session Management                            │
│  • Request Validation                            │
└─────────────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────┐
│              Prisma ORM Layer                    │
│  • User Service                                  │
│  • Partner Service                               │
│  • Product Service                               │
│  • Order Service                                 │
└─────────────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────┐
│              MongoDB Database                    │
└─────────────────────────────────────────────────┘
```

---

## 📦 Функциональные модули

### 1. 🔐 Аутентификация и авторизация

#### Функционал:
- Страница входа с паролем
- Сессионная аутентификация
- Защита всех маршрутов middleware
- Автоматическое перенаправление на /login

#### Поля:
```typescript
Session {
  isAdmin: boolean
  // Возможно расширение для ролей
}
```

#### Endpoint:
- `GET /admin/login` - Страница входа
- `POST /admin/login` - Обработка входа
- `GET /admin/logout` - Выход из системы (TODO)

---

### 2. 🏠 Главная панель (Dashboard)

#### Функционал:
- **Статистика в реальном времени**:
  - Общий баланс всех пользователей
  - Количество категорий товаров
  - Количество товаров
  - Количество партнеров
  - Количество отзывов
  
- **Быстрые действия**:
  - Отправка сообщений пользователям
  - Добавление нового товара
  - Создание категории
  - Переход в разделы управления

- **Последние события**:
  - Последние 5 пользователей
  - Последние 5 заказов
  - Последние 5 транзакций

#### Визуализация:
```
┌────────────────────────────────────────────┐
│  📊 Dashboard                              │
├────────────────────────────────────────────┤
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐  │
│  │ 💰   │  │ 📁   │  │ 🛍   │  │ 🤝   │  │
│  │12.3K │  │  15  │  │  87  │  │ 234  │  │
│  │ PZ   │  │ CAT  │  │ PROD │  │ PART │  │
│  └──────┘  └──────┘  └──────┘  └──────┘  │
├────────────────────────────────────────────┤
│  Быстрые действия:                         │
│  [📨 Сообщение] [➕ Товар] [📂 Категория] │
├────────────────────────────────────────────┤
│  📋 Последняя активность                   │
│  └─ Таблица с фильтрами                   │
└────────────────────────────────────────────┘
```

#### Endpoint:
- `GET /admin` - Главная страница

---

### 3. 👥 Управление пользователями

#### 3.1 Основной список пользователей

**Функционал:**
- Сортировка по:
  - Балансу (по убыванию/возрастанию)
  - Количеству партнеров
  - Сумме заказов
  - Дате регистрации
  
- Фильтрация:
  - Поиск по username (автодополнение)
  - По региону (Россия/Бали)
  - По статусу партнера
  
- Постраничная навигация
  - По умолчанию: первые 50 пользователей
  - Загрузка всех (кнопка "Показать всех")

**Отображаемые данные:**
```typescript
UserRow {
  avatar: string          // Аватар (первая буква имени)
  name: string           // ФИО
  username: string       // @username
  inviter: string        // Кто пригласил
  balance: number        // Баланс в PZ
  bonus: number          // Бонусы
  partners: {
    total: number        // Всего партнеров
    direct: number       // Прямых партнеров
  }
  orders: {
    sum: number          // Сумма заказов
    count: number        // Количество заказов
    status: 'new' | 'processing' | 'completed' | 'cancelled'
  }
  lastActivity: Date     // Последняя активность
  actions: Action[]      // Действия с пользователем
}
```

**Визуальные индикаторы:**
- 🔴 Новый заказ
- 🟡 Заказ в обработке
- 🟢 Завершенные заказы
- ⚫ Отмененные заказы

**Действия с пользователем:**
- 👁 Подробная информация
- 🌳 Иерархия партнеров
- 🔄 Сменить пригласителя
- 💰 Управление балансом
- 📦 Просмотр заказов
- 👥 Просмотр партнеров

#### 3.2 Детальная карточка пользователя

**Разделы:**

**📊 Основная информация:**
- Баланс
- Бонусы
- Количество партнеров
- Сумма заказов
- Дата регистрации
- Последняя активность
- **📍 Адрес доставки** (редактируемый)

**🤝 Партнёрская информация:**
- Статус активации
- Тип программы (Direct/Multi-level)
- Прямых партнеров
- Всего партнеров (включая 2-й и 3-й уровень)
- Реферальный код
- Срок действия активации

**🛒 Последние заказы:**
- Таблица с заказами
- Статус, сумма, дата
- Клик для детального просмотра

**💰 Последние транзакции:**
- История начислений/списаний
- Тип операции, сумма, описание

**📈 Последние действия:**
- Журнал активности пользователя
- Действие, дата/время

**Управление:**
- Активация партнерской программы
- Выбор типа программы (Direct/Multi)
- Указание срока активации (месяцы)
- Управление балансом (пополнение/списание)

#### 3.3 Смена пригласителя

**Функционал:**
- Модальное окно с поиском
- Поиск по username (с @)
- Поиск по реферальному коду
- Автодополнение с подсказками
- Валидация перед сменой
- Подтверждение действия

**Процесс:**
1. Открытие модального окна
2. Ввод username или кода
3. Выбор из предложенных вариантов
4. Подтверждение смены
5. Обновление данных в БД
6. Перерасчет бонусов (опционально)

#### 3.4 Управление балансом

**Операции:**
- ➕ Пополнение баланса
- ➖ Списание баланса

**Поля:**
```typescript
BalanceOperation {
  userId: string
  operation: 'add' | 'subtract'
  amount: number
  comment: string       // Причина операции
  timestamp: Date
}
```

**Функционал:**
- Модальное окно
- Валидация суммы (> 0)
- Обязательный комментарий
- История операций
- Отправка уведомления пользователю (TODO)

#### Endpoints:
- `GET /admin/users-detailed` - Список пользователей
- `GET /admin/users/:userId` - Карточка пользователя
- `GET /admin/users/search` - Поиск по username
- `GET /admin/inviters/search` - Поиск пригласителей
- `POST /admin/users/:id/change-inviter` - Смена пригласителя
- `POST /admin/users/:userId/update-balance` - Обновление баланса
- `POST /admin/users/:userId/activate-referral` - Активация реферальной программы
- `POST /admin/users/:userId/delivery-address` - Обновление адреса доставки
- `GET /admin/users/:userId/partners` - Партнеры пользователя
- `GET /admin/users/:userId/orders` - Заказы пользователя

---

### 4. 🤝 Управление партнерской программой

#### 4.1 Список партнеров

**Функционал:**
- Таблица всех партнеров
- Информация о каждом:
  - ФИО и username
  - Реферальный код
  - Информация о пригласителе
  - Тип программы
  - Статус активации
  - Срок действия
  - Баланс и бонусы
  - Количество партнеров (1-й, 2-й, 3-й уровень)
  
- Индикаторы статуса:
  - ✅ Активен (зеленый)
  - ⏳ Истекает через N дней (желтый)
  - ⚠️ Истекает сегодня/завтра (оранжевый)
  - ❌ Неактивен (красный)

**Быстрые действия:**
- Просмотр иерархии
- Управление балансом
- Активация/деактивация
- Тестирование реферальных ссылок

#### 4.2 Иерархия партнеров

**Визуализация:**
```
👑 Roman (lvl 0) - 15.45 PZ
  ├─ 🥈 Ivan (lvl 1) - 8.30 PZ
  │   ├─ 🥉 Petr (lvl 2) - 3.20 PZ
  │   └─ 🥉 Anna (lvl 2) - 1.50 PZ
  └─ 🥈 Maria (lvl 1) - 5.60 PZ
      └─ 🥉 Olga (lvl 2) - 2.10 PZ
```

**Функционал:**
- Древовидное отображение
- Фильтр по конкретному пользователю
- Показ всей структуры
- Балансы на каждом уровне
- Клик для детального просмотра

#### 4.3 Тестирование реферальных ссылок

**Функционал:**
- Генерация тестовых ссылок
- Проверка парсинга ссылок
- Отображение обеих форматов (short/full)
- Инструкции по тестированию

#### Endpoints:
- `GET /admin/partners` - Список партнеров
- `GET /admin/partners-hierarchy` - Иерархия
- `GET /admin/test-referral-links` - Тестирование ссылок
- `GET /admin/debug-partners` - Отладка (debug)
- `POST /admin/fix-roman-bonuses` - Исправление бонусов (специфичная функция)

---

### 5. 🛍 Управление товарами

#### 5.1 Каталог товаров

**Функционал:**
- Таблица всех товаров
- Группировка по категориям
- Фильтрация:
  - По категории
  - По статусу (активен/неактивен)
  - По наличию на складе
  - По региону (Россия/Бали)

**Отображаемые данные:**
```typescript
ProductRow {
  image: string          // Изображение товара
  title: string          // Название
  category: string       // Категория
  price: number          // Цена в PZ
  stock: number          // Остаток на складе
  regions: {
    russia: boolean      // Доступен в РФ
    bali: boolean        // Доступен на Бали
  }
  isActive: boolean      // Активен/неактивен
  createdAt: Date        // Дата создания
  actions: Action[]      // Действия с товаром
}
```

**Действия:**
- ✏️ Редактировать
- 🗑 Удалить
- 👁 Просмотр в боте (TODO)

#### 5.2 Добавление товара

**Форма (модальное окно):**
```typescript
ProductForm {
  title: string          // Название *обязательное
  summary: string        // Краткое описание *обязательное
  description: string    // Полное описание
  price: number          // Цена в PZ *обязательное
  categoryId: string     // ID категории *обязательное
  image: File            // Изображение (upload)
  stock: number          // Остаток (по умолчанию 999)
  availableInRussia: boolean  // Доступен в РФ
  availableInBali: boolean    // Доступен на Бали
  isActive: boolean      // Активен при создании
}
```

**Процесс загрузки изображения:**
1. Выбор файла (макс. 5MB)
2. Загрузка на Cloudinary
3. Получение URL
4. Сохранение в БД

**Валидация:**
- Все обязательные поля заполнены
- Цена > 0
- Изображение в формате JPG/PNG
- Размер файла ≤ 5MB

#### 5.3 Редактирование товара

**Функционал:**
- Модальное окно с формой
- Предзаполнение текущими данными
- Возможность изменить изображение
- Сохранение изменений
- Удаление товара

#### Endpoints:
- `GET /admin/products` - Список товаров
- `POST /admin/products` - Создание товара (с upload)
- `PUT /admin/products/:id` - Обновление товара
- `DELETE /admin/products/:id` - Удаление товара

---

### 6. 📂 Управление категориями

#### Функционал:
- Таблица категорий
- Количество товаров в каждой
- Статус активности

**Данные категории:**
```typescript
Category {
  id: string
  name: string           // Название
  slug: string           // URL-slug
  description: string    // Описание
  isActive: boolean      // Активна/неактивна
  productCount: number   // Количество товаров
  createdAt: Date
  updatedAt: Date
}
```

**Действия:**
- Создание категории
- Редактирование (TODO)
- Удаление (TODO)
- Активация/деактивация (TODO)

#### Endpoints:
- `GET /admin/categories` - Список категорий
- `GET /admin/api/categories` - API получения категорий
- `POST /admin/api/categories` - Создание категории

---

### 7. 📦 Управление заказами

#### 7.1 Список заказов

**Функционал:**
- Таблица всех заказов
- Фильтрация по статусу:
  - 🔴 NEW (Новый)
  - 🟡 PROCESSING (В обработке)
  - 🟢 COMPLETED (Завершен)
  - ⚫ CANCELLED (Отменен)

**Отображаемые данные:**
```typescript
OrderRow {
  id: string
  orderNumber: string    // #ORDER-001
  user: {
    name: string
    username: string
    telegramId: string
  }
  items: OrderItem[]     // Список товаров
  totalAmount: number    // Общая сумма
  status: OrderStatus
  message: string        // Комментарий клиента
  createdAt: Date
  actions: Action[]
}

OrderItem {
  productId: string
  title: string
  price: number
  quantity: number
}
```

**Группировка:**
- По статусам в отдельных секциях
- Счетчик заказов в каждой группе
- Цветовая индикация

**Действия:**
- Просмотр деталей
- Изменение статуса (кнопки)
- Редактирование заказа
- Удаление заказа
- Отметка как оплаченный
- Связь с клиентом (Telegram link)

#### 7.2 Изменение статуса заказа

**Быстрая смена статуса:**
- Кнопки с эмодзи:
  - 🔴 Новый
  - 🟡 В обработке
  - 🟢 Готово
  - ⚫ Отмена
  
**Автоматизация:**
- Обновление в БД
- Отправка уведомления клиенту (TODO)
- Логирование изменения

#### 7.3 Редактирование заказа

**Функционал:**
- Модальное окно
- Изменение состава заказа:
  - Добавление товаров
  - Удаление товаров
  - Изменение количества
  - Автоматический пересчет суммы
  
- Изменение контактных данных
- Изменение комментария
- История изменений (TODO)

#### 7.4 Адрес доставки

**Функционал:**
- Отображение в карточке заказа
- Редактирование через пользователя
- Типы адресов:
  - 🇮🇩 Бали (район, вилла)
  - 🇷🇺 РФ (город, адрес)
  - ✏️ Произвольный

**Формат хранения:**
```
"Тип: Адрес"
Например: "Бали: Убуд, Villa Sungai"
```

#### Endpoints:
- `GET /admin/orders` - Список заказов
- `GET /admin/orders/:orderId` - Детали заказа
- `PUT /admin/orders/:orderId/status` - Изменение статуса
- `PUT /admin/orders/:orderId` - Редактирование заказа
- `DELETE /admin/orders/:orderId` - Удаление заказа
- `POST /admin/orders/:orderId/payment` - Отметка оплаты

---

### 8. ⭐ Управление отзывами

**Функционал:**
- Таблица отзывов
- Информация:
  - Имя автора
  - Фото (опционально)
  - Текст отзыва
  - Ссылка (опционально)
  - Закреплен (isPinned)
  - Активен (isActive)
  - Дата создания

**Действия:**
- Создание отзыва
- Редактирование
- Удаление
- Закрепление/открепление
- Активация/деактивация

**Данные отзыва:**
```typescript
Review {
  id: string
  name: string           // Имя автора
  photoUrl: string       // URL фото
  content: string        // Текст отзыва
  link: string          // Ссылка (опционально)
  isPinned: boolean     // Закреплен
  isActive: boolean     // Активен
  createdAt: Date
  updatedAt: Date
}
```

#### Endpoints:
- `GET /admin/reviews` - Список отзывов
- `POST /admin/reviews` - Создание отзыва
- `PUT /admin/reviews/:id` - Обновление
- `DELETE /admin/reviews/:id` - Удаление

---

### 9. 📨 Массовая рассылка

**Функционал:**
- Модальное окно отправки
- Выбор получателей:
  - Все пользователи
  - Выбранные вручную (checkbox)
  - По критериям (TODO):
    - Партнеры
    - По региону
    - По балансу
    - Активные за период
  
**Форма сообщения:**
```typescript
MessageBroadcast {
  recipients: string[]   // ID пользователей
  message: string        // Текст сообщения
  attachments: {        // Вложения (TODO)
    images: string[]
    files: string[]
  }
  schedule: Date        // Отложенная отправка (TODO)
}
```

**Процесс:**
1. Выбор получателей
2. Написание сообщения
3. Предпросмотр (TODO)
4. Отправка
5. Отчет об отправке:
   - Успешно отправлено: N
   - Ошибки: M
   - Список ошибок

**Ограничения:**
- Максимум 30 пользователей за раз (Telegram API)
- Задержка между сообщениями (anti-flood)
- Логирование всех рассылок

#### Endpoints:
- `POST /admin/send-message` - Отправка сообщения

---

### 10. 📊 Аналитика и статистика

**Текущая реализация:**
- Основные счетчики на главной странице
- Последние события

**Рекомендуемые улучшения:**

#### 10.1 Дашборд аналитики

**Метрики:**
```typescript
Analytics {
  revenue: {
    total: number          // Общая выручка
    today: number          // За сегодня
    week: number           // За неделю
    month: number          // За месяц
    growth: number         // Рост в %
  }
  
  users: {
    total: number          // Всего пользователей
    new: number            // Новых за период
    active: number         // Активных
    retention: number      // Удержание в %
  }
  
  orders: {
    total: number          // Всего заказов
    new: number            // Новых
    processing: number     // В обработке
    completed: number      // Завершенных
    cancelled: number      // Отмененных
    avgValue: number       // Средний чек
  }
  
  partners: {
    active: number         // Активных партнеров
    inactive: number       // Неактивных
    totalReferred: number  // Всего рефералов
    conversionRate: number // Конверсия в %
  }
}
```

#### 10.2 Графики и визуализация

**Графики (рекомендуется добавить):**
- 📈 График продаж по дням
- 👥 Рост базы пользователей
- 🤝 Активность партнеров
- 💰 Динамика балансов
- 📦 Статусы заказов (pie chart)

**Инструменты:**
- Chart.js / D3.js
- Экспорт в Excel/CSV
- Настраиваемые периоды

---

## 🎨 Интерфейс и UX

### Дизайн-система

#### Цветовая палитра
```css
/* Основные цвета */
--primary: #007bff;        /* Синий - основной */
--success: #28a745;        /* Зеленый - успех */
--warning: #ffc107;        /* Желтый - внимание */
--danger: #dc3545;         /* Красный - опасность */
--info: #17a2b8;          /* Голубой - информация */

/* Статусы заказов */
--status-new: #dc3545;     /* Красный */
--status-processing: #ffc107; /* Желтый */
--status-completed: #28a745;  /* Зеленый */
--status-cancelled: #6c757d;  /* Серый */

/* Нейтральные */
--gray-100: #f8f9fa;
--gray-200: #e9ecef;
--gray-600: #6c757d;
--gray-900: #212529;

/* Фон и текст */
--bg-primary: #ffffff;
--bg-secondary: #f5f5f5;
--text-primary: #333333;
--text-secondary: #666666;
```

#### Типографика
```css
/* Заголовки */
h1: 32px, bold
h2: 28px, bold
h3: 24px, semibold
h4: 20px, semibold

/* Текст */
body: 14px, regular
small: 12px, regular
tiny: 11px, regular

/* Семейство шрифтов */
font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Arial, sans-serif;
```

#### Компоненты

**Кнопки:**
```css
.btn {
  padding: 8px 16px;
  border-radius: 4px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s;
}

.btn-primary { background: var(--primary); }
.btn-success { background: var(--success); }
.btn-danger { background: var(--danger); }
```

**Карточки:**
```css
.card {
  background: white;
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  padding: 20px;
}
```

**Таблицы:**
```css
.table {
  width: 100%;
  border-collapse: collapse;
}

.table th {
  text-align: left;
  padding: 12px;
  background: var(--gray-100);
  font-weight: 600;
}

.table td {
  padding: 12px;
  border-bottom: 1px solid var(--gray-200);
}

.table tr:hover {
  background: var(--gray-100);
}
```

**Модальные окна:**
```css
.modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0,0,0,0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 10000;
}

.modal-content {
  background: white;
  border-radius: 8px;
  padding: 20px;
  max-width: 600px;
  max-height: 80vh;
  overflow-y: auto;
}
```

### Адаптивность

**Breakpoints:**
```css
/* Mobile First Approach */
@media (min-width: 640px)  { /* sm */ }
@media (min-width: 768px)  { /* md */ }
@media (min-width: 1024px) { /* lg */ }
@media (min-width: 1280px) { /* xl */ }
```

**Рекомендации:**
- Mobile: Одноколоночный layout
- Tablet: Двухколоночный layout
- Desktop: Трехколоночный layout
- Адаптивные таблицы (горизонтальный скролл на мобильных)

### UX паттерны

#### Обратная связь пользователю
```javascript
// Success
alert('✅ Успешно сохранено');

// Error
alert('❌ Ошибка: ' + error.message);

// Loading
button.disabled = true;
button.textContent = 'Загрузка...';
```

#### Подтверждение действий
```javascript
// Для критичных операций
if (confirm('Вы уверены, что хотите удалить?')) {
  // Выполнить удаление
}
```

#### Валидация форм
```javascript
// Проверка обязательных полей
if (!title || !price || !categoryId) {
  alert('Заполните все обязательные поля');
  return;
}

// Проверка формата
if (price <= 0) {
  alert('Цена должна быть больше 0');
  return;
}
```

---

## 🔒 Безопасность

### 1. Аутентификация

**Текущая реализация:**
```typescript
const requireAdmin = (req, res, next) => {
  if (!req.session.isAdmin) {
    return res.redirect('/admin/login');
  }
  next();
};
```

**Рекомендации по улучшению:**
- Добавить JWT токены
- Реализовать refresh tokens
- Добавить rate limiting на login
- Логирование всех попыток входа
- 2FA (двухфакторная аутентификация)

### 2. Авторизация (роли)

**Предлагаемая система ролей:**
```typescript
enum AdminRole {
  SUPER_ADMIN = 'super_admin',    // Полный доступ
  ADMIN = 'admin',                 // Все кроме настроек
  MANAGER = 'manager',             // Заказы + пользователи
  SUPPORT = 'support',             // Только просмотр
}

interface AdminUser {
  id: string
  email: string
  role: AdminRole
  permissions: Permission[]
  lastLogin: Date
}
```

**Матрица прав доступа:**
```
┌─────────────────┬───────┬───────┬─────────┬─────────┐
│ Функция         │ Super │ Admin │ Manager │ Support │
├─────────────────┼───────┼───────┼─────────┼─────────┤
│ Управление      │   ✅  │   ✅  │    ❌   │    ❌   │
│ пользователями  │       │       │         │         │
├─────────────────┼───────┼───────┼─────────┼─────────┤
│ Заказы          │   ✅  │   ✅  │    ✅   │    👁   │
├─────────────────┼───────┼───────┼─────────┼─────────┤
│ Товары          │   ✅  │   ✅  │    ✅   │    👁   │
├─────────────────┼───────┼───────┼─────────┼─────────┤
│ Партнеры        │   ✅  │   ✅  │    👁   │    👁   │
├─────────────────┼───────┼───────┼─────────┼─────────┤
│ Рассылка        │   ✅  │   ✅  │    ❌   │    ❌   │
├─────────────────┼───────┼───────┼─────────┼─────────┤
│ Настройки       │   ✅  │   ❌  │    ❌   │    ❌   │
└─────────────────┴───────┴───────┴─────────┴─────────┘
```

### 3. Защита данных

**Валидация входных данных:**
```typescript
import { body, param, validationResult } from 'express-validator';

// Пример валидации
router.post('/products', [
  body('title').trim().notEmpty().isLength({ max: 200 }),
  body('price').isFloat({ min: 0 }),
  body('categoryId').isMongoId(),
], async (req, res) => {
  const errors = validationResult(req);
  if (!errors.isEmpty()) {
    return res.status(400).json({ errors: errors.array() });
  }
  // Обработка...
});
```

**XSS Protection:**
```typescript
import xss from 'xss';

const sanitized = xss(userInput);
```

**CSRF Protection:**
```typescript
import csrf from 'csurf';

const csrfProtection = csrf({ cookie: true });
router.post('/action', csrfProtection, handler);
```

### 4. Логирование

**Рекомендуемая структура логов:**
```typescript
interface AuditLog {
  id: string
  timestamp: Date
  userId: string
  action: string           // 'create', 'update', 'delete', 'view'
  resource: string         // 'user', 'order', 'product'
  resourceId: string
  changes: {
    before: any
    after: any
  }
  ip: string
  userAgent: string
}
```

**Что логировать:**
- Все попытки входа (успешные и нет)
- Изменения критичных данных
- Доступ к конфиденциальной информации
- Ошибки и исключения
- API запросы

---

## 🔌 API Endpoints

### Полный список маршрутов

#### Аутентификация
```
GET  /admin/login              - Страница входа
POST /admin/login              - Обработка входа
GET  /admin/logout             - Выход (TODO)
```

#### Главная панель
```
GET  /admin                    - Dashboard
```

#### Пользователи
```
GET  /admin/users-detailed                    - Список пользователей
GET  /admin/users/:userId                     - Карточка пользователя
GET  /admin/users/search                      - Поиск по username
GET  /admin/inviters/search                   - Поиск пригласителей
POST /admin/users/:id/change-inviter          - Смена пригласителя
POST /admin/users/:userId/update-balance      - Обновление баланса
POST /admin/users/:userId/activate-referral   - Активация реферальной программы
POST /admin/users/:userId/delivery-address    - Обновление адреса доставки
GET  /admin/users/:userId/partners            - Партнеры пользователя
GET  /admin/users/:userId/orders              - Заказы пользователя
GET  /admin/users/:userId/card                - Карточка с транзакциями
```

#### Партнеры
```
GET  /admin/partners                   - Список партнеров
GET  /admin/partners-hierarchy         - Иерархия партнеров
GET  /admin/test-referral-links        - Тест реферальных ссылок
GET  /admin/debug-partners             - Отладка партнеров
POST /admin/fix-roman-bonuses          - Исправление бонусов
```

#### Товары
```
GET    /admin/products              - Список товаров
POST   /admin/products              - Создание товара (multipart/form-data)
PUT    /admin/products/:id          - Обновление товара
DELETE /admin/products/:id          - Удаление товара
```

#### Категории
```
GET  /admin/categories              - Список категорий
GET  /admin/api/categories          - API получения категорий
POST /admin/api/categories          - Создание категории
```

#### Заказы
```
GET    /admin/orders                      - Список заказов
GET    /admin/orders/:orderId             - Детали заказа
PUT    /admin/orders/:orderId/status      - Изменение статуса
PUT    /admin/orders/:orderId             - Редактирование заказа
DELETE /admin/orders/:orderId             - Удаление заказа
POST   /admin/orders/:orderId/payment     - Отметка оплаты
```

#### Отзывы
```
GET    /admin/reviews           - Список отзывов
POST   /admin/reviews           - Создание отзыва
PUT    /admin/reviews/:id       - Обновление отзыва
DELETE /admin/reviews/:id       - Удаление отзыва
```

#### Рассылка
```
POST /admin/send-message        - Массовая рассылка
```

#### Тестирование и отладка
```
GET /admin/test-dual-system        - Тест двойной системы бонусов
GET /admin/debug-user/:userId      - Отладка пользователя
GET /admin/debug-user-full/:userId - Полная отладка пользователя
```

---

## 💾 База данных

### Схема данных (MongoDB + Prisma)

#### User
```prisma
model User {
  id              String          @id @default(auto()) @map("_id") @db.ObjectId
  telegramId      String          @unique
  firstName       String?
  lastName        String?
  username        String?
  languageCode    String?
  phone           String?
  selectedRegion  Region?         @default(RUSSIA)
  deliveryAddress String?         // Адрес доставки
  balance         Float           @default(0)
  cartItems       CartItem[]
  histories       UserHistory[]
  partner         PartnerProfile?
  orders          OrderRequest[]
  createdAt       DateTime        @default(now())
  updatedAt       DateTime        @updatedAt
}
```

#### PartnerProfile
```prisma
model PartnerProfile {
  id             String              @id @default(auto()) @map("_id") @db.ObjectId
  user           User                @relation(fields: [userId], references: [id])
  userId         String              @unique @db.ObjectId
  isActive       Boolean             @default(false)
  activatedAt    DateTime?
  expiresAt      DateTime?
  activationType String?             // 'PURCHASE' или 'ADMIN'
  programType    PartnerProgramType  @default(DIRECT)
  referralCode   String              @unique
  balance        Float               @default(0)
  bonus          Float               @default(0)
  totalPartners  Int                 @default(0)
  directPartners Int                 @default(0)
  multiPartners  Int                 @default(0)
  referrals      PartnerReferral[]
  transactions   PartnerTransaction[]
  createdAt      DateTime            @default(now())
  updatedAt      DateTime            @updatedAt
}
```

#### Product
```prisma
model Product {
  id                String     @id @default(auto()) @map("_id") @db.ObjectId
  title             String
  summary           String
  description       String?
  imageUrl          String?
  price             Float
  stock             Int        @default(999)
  isActive          Boolean    @default(true)
  availableInRussia Boolean    @default(true)
  availableInBali   Boolean    @default(true)
  category          Category   @relation(fields: [categoryId], references: [id])
  categoryId        String     @db.ObjectId
  cartItems         CartItem[]
  createdAt         DateTime   @default(now())
  updatedAt         DateTime   @updatedAt
}
```

#### OrderRequest
```prisma
model OrderRequest {
  id          String   @id @default(auto()) @map("_id") @db.ObjectId
  user        User?    @relation(fields: [userId], references: [id])
  userId      String?  @db.ObjectId
  contact     String?
  message     String
  itemsJson   Json     // Список товаров
  status      String   @default("NEW")  // NEW, PROCESSING, COMPLETED, CANCELLED
  createdAt   DateTime @default(now())
}
```

#### Category
```prisma
model Category {
  id          String     @id @default(auto()) @map("_id") @db.ObjectId
  name        String
  slug        String     @unique
  description String?
  isActive    Boolean    @default(true)
  products    Product[]
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
}
```

#### Review
```prisma
model Review {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  name      String
  photoUrl  String?
  content   String
  link      String?
  isPinned  Boolean  @default(false)
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
```

### Индексы

**Рекомендуемые индексы для оптимизации:**
```typescript
// User
@@index([telegramId])
@@index([username])
@@index([createdAt])

// PartnerProfile
@@index([referralCode])
@@index([userId])
@@index([isActive, expiresAt])

// Product
@@index([categoryId])
@@index([isActive])
@@index([availableInRussia, availableInBali])

// OrderRequest
@@index([userId])
@@index([status])
@@index([createdAt])
```

---

## 🚀 Возможные улучшения

### 1. Краткосрочные (Priority: High)

#### 1.1 Аналитика
- [ ] Dashboard с графиками (Chart.js)
- [ ] Экспорт данных в CSV/Excel
- [ ] Фильтры по датам
- [ ] Сравнение периодов

#### 1.2 Уведомления
- [ ] Push-уведомления админам о новых заказах
- [ ] Email-уведомления (при интеграции email)
- [ ] Webhook для интеграций

#### 1.3 Поиск и фильтрация
- [ ] Глобальный поиск по всей админке
- [ ] Расширенные фильтры пользователей
- [ ] Сохраненные фильтры

#### 1.4 Массовые операции
- [ ] Массовое изменение статусов заказов
- [ ] Массовое редактирование товаров
- [ ] Массовая активация партнеров

### 2. Среднесрочные (Priority: Medium)

#### 2.1 CRM функционал
- [ ] Заметки к пользователям
- [ ] Теги и сегментация
- [ ] История взаимодействий
- [ ] Воронка продаж

#### 2.2 Автоматизация
- [ ] Автоматическая смена статусов заказов
- [ ] Напоминания о неоплаченных заказах
- [ ] Автоматическая деактивация партнеров
- [ ] Шаблоны сообщений

#### 2.3 Финансы
- [ ] Детальный учет транзакций
- [ ] Отчеты по выручке
- [ ] Прогнозирование
- [ ] Интеграция с бухгалтерией

#### 2.4 Логистика
- [ ] Управление складом
- [ ] Трекинг доставки
- [ ] Интеграция с службами доставки
- [ ] Расчет стоимости доставки

### 3. Долгосрочные (Priority: Low)

#### 3.1 Расширенная аналитика
- [ ] ML-прогнозирование продаж
- [ ] Анализ поведения пользователей
- [ ] A/B тестирование
- [ ] Когортный анализ

#### 3.2 Многопользовательская работа
- [ ] Система ролей и прав
- [ ] Логи действий администраторов
- [ ] Разграничение доступа
- [ ] Совместная работа над заказами

#### 3.3 Интеграции
- [ ] 1C
- [ ] CRM системы (AmoCRM, Bitrix24)
- [ ] Email маркетинг (Mailchimp)
- [ ] Аналитика (Google Analytics, Yandex Metrica)
- [ ] Платежные системы

#### 3.4 Мобильное приложение
- [ ] React Native app для админов
- [ ] Push-уведомления
- [ ] Быстрые действия
- [ ] Офлайн режим

### 4. Технические улучшения

#### 4.1 Performance
- [ ] Кэширование (Redis)
- [ ] Пагинация всех списков
- [ ] Ленивая загрузка
- [ ] Оптимизация запросов к БД

#### 4.2 UX/UI
- [ ] Переход на React/Vue
- [ ] Темная тема
- [ ] Кастомизация интерфейса
- [ ] Горячие клавиши

#### 4.3 DevOps
- [ ] CI/CD пайплайн
- [ ] Мониторинг (Sentry, Prometheus)
- [ ] Автоматическое тестирование
- [ ] Backup стратегия

---

## 📝 Чек-лист для разработки новой админки

### Этап 1: Планирование
- [ ] Анализ требований
- [ ] Определение ролей пользователей
- [ ] Проектирование БД
- [ ] Выбор технологий
- [ ] Создание прототипов

### Этап 2: Backend
- [ ] Настройка сервера (Express)
- [ ] Интеграция БД (Prisma + MongoDB)
- [ ] API endpoints
- [ ] Middleware (auth, validation)
- [ ] Бизнес-логика
- [ ] Тесты

### Этап 3: Frontend
- [ ] Верстка компонентов
- [ ] Формы и валидация
- [ ] Модальные окна
- [ ] Таблицы с сортировкой
- [ ] Графики и визуализация
- [ ] Адаптивность

### Этап 4: Интеграция
- [ ] Подключение к Telegram Bot
- [ ] File Upload (Cloudinary)
- [ ] Email (если нужно)
- [ ] Платежные системы (если нужно)

### Этап 5: Тестирование
- [ ] Unit tests
- [ ] Integration tests
- [ ] E2E tests
- [ ] Load testing
- [ ] Security audit

### Этап 6: Деплой
- [ ] Настройка production окружения
- [ ] SSL сертификаты
- [ ] Мониторинг
- [ ] Backup
- [ ] Документация

---

## 🔗 Полезные ссылки

### Документация
- [Express.js](https://expressjs.com/)
- [Prisma](https://www.prisma.io/docs/)
- [MongoDB](https://docs.mongodb.com/)
- [Telegram Bot API](https://core.telegram.org/bots/api)

### Инструменты
- [Postman](https://www.postman.com/) - тестирование API
- [MongoDB Compass](https://www.mongodb.com/products/compass) - GUI для MongoDB
- [Cloudinary](https://cloudinary.com/) - хранение изображений

### Библиотеки для улучшения
- [Chart.js](https://www.chartjs.org/) - графики
- [DataTables](https://datatables.net/) - продвинутые таблицы
- [Select2](https://select2.org/) - улучшенные select'ы
- [SweetAlert2](https://sweetalert2.github.io/) - красивые алерты

---

## 📞 Контакты и поддержка

При разработке новой админки рекомендуется:
1. Создать отдельный репозиторий
2. Настроить CI/CD
3. Документировать все изменения
4. Проводить code review
5. Регулярно обновлять зависимости

---

**Версия документа:** 1.0
**Дата создания:** 2025-01-10
**Последнее обновление:** 2025-01-10
**Статус:** Актуально

---

*Этот документ создан на основе анализа текущей реализации админ-панели Vital Bot и содержит рекомендации по ее развитию и улучшению.*

