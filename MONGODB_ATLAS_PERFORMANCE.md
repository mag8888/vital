# MongoDB Atlas M0: Производительность и Лимиты

## Лимиты бесплатного плана M0

### Ресурсы
- **Storage**: 512MB (достаточно для ~50,000-100,000 документов)
- **RAM**: Shared (не выделенная, зависит от нагрузки других пользователей)
- **CPU**: Shared (не выделенный)
- **Соединения**: До 500 одновременных подключений

### Производительность
- ⚠️ **Может быть медленнее** при высокой нагрузке (shared ресурсы)
- ✅ **Обычно достаточно** для небольших/средних проектов
- ⚠️ **Может тормозить** при пиковых нагрузках (много одновременных запросов)

## Оценка для вашего проекта

### Ваша нагрузка (примерно):
- Telegram бот с веб-приложением
- Обычные CRUD операции (создание пользователей, товаров, заказов)
- Нет сложных аналитических запросов
- Нет больших объемов данных

### Вывод:
✅ **M0 должно быть достаточно** для начала и первых месяцев работы

### Когда M0 может не хватить:
- 🚨 Более 1000 активных пользователей одновременно
- 🚨 Более 10,000 запросов в минуту
- 🚨 Большие объемы данных (>400MB)
- 🚨 Сложные аналитические запросы

## Рекомендации

### Вариант 1: Начать с M0 (рекомендуется)
1. Использовать M0 для начала
2. Мониторить производительность через Atlas Dashboard
3. При необходимости перейти на M2 ($9/месяц) или M5 ($25/месяц)

### Вариант 2: Сразу использовать M2 ($9/месяц)
- Выделенные ресурсы (2GB RAM, 10GB storage)
- Гарантированная производительность
- Лучше для продакшена

### Вариант 3: Оптимизировать код
- Использовать индексы для частых запросов
- Кэшировать часто запрашиваемые данные
- Оптимизировать запросы (не загружать лишние данные)

## Оптимизация для M0

### 1. Добавить индексы в Prisma schema:
```prisma
model User {
  telegramId String @unique  // ✅ Уже есть индекс
  // Добавить индексы для частых запросов
  @@index([createdAt])
}

model Product {
  @@index([isActive, categoryId])  // Для фильтрации товаров
  @@index([createdAt])
}

model OrderRequest {
  @@index([userId, createdAt])
  @@index([status])
}
```

### 2. Использовать select для загрузки только нужных полей:
```typescript
// Вместо загрузки всех полей
const user = await prisma.user.findUnique({ where: { id } });

// Загружать только нужные поля
const user = await prisma.user.findUnique({
  where: { id },
  select: { id: true, telegramId: true, balance: true }
});
```

### 3. Кэшировать статичные данные:
- Список товаров (обновлять раз в минуту)
- Категории (кэшировать в памяти)
- Контент бота (кэшировать)

## Мониторинг производительности

### В MongoDB Atlas Dashboard:
1. Откройте ваш кластер
2. Перейдите в **"Metrics"**
3. Следите за:
   - **CPU Usage** (должно быть <80%)
   - **Memory Usage** (должно быть <80%)
   - **Disk I/O** (должно быть низким)
   - **Connection Count** (должно быть <400)

### Performance Advisor:
- Atlas автоматически анализирует медленные запросы
- Предлагает индексы для оптимизации
- Используйте эти рекомендации!

## План действий

1. ✅ **Начать с M0** (бесплатно)
2. 📊 **Мониторить производительность** первые 2 недели
3. 🔍 **Использовать Performance Advisor** для оптимизации
4. ⚡ **Добавить индексы** по рекомендациям
5. 📈 **При росте нагрузки** перейти на M2 ($9/месяц)

## Вывод

**Для вашего проекта M0 должно быть достаточно**, если:
- У вас <1000 активных пользователей
- Нет пиковых нагрузок (много одновременных запросов)
- Данные <400MB

**Если заметите тормоза:**
- Сначала оптимизируйте код (индексы, кэш)
- Если не поможет - перейдите на M2 ($9/месяц)

---

## Сравнение планов

| План | Цена | RAM | Storage | Производительность |
|------|------|-----|---------|-------------------|
| M0   | Бесплатно | Shared | 512MB | Может тормозить при нагрузке |
| M2   | $9/мес | 2GB | 10GB | Стабильная |
| M5   | $25/мес | 4GB | 20GB | Высокая |

**Рекомендация**: Начните с M0, при необходимости перейдите на M2.




