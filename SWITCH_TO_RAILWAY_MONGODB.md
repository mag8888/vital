# 🔄 Переключение на Railway MongoDB

## ⚠️ Текущая проблема

Бот использует MongoDB Atlas (`mongodb+srv://PLAZMA...`), который недоступен или медленный. Нужно переключиться на Railway MongoDB.

## 🚀 Быстрое переключение (3 шага)

### Шаг 1: Добавьте MongoDB на Railway

1. Откройте [Railway Dashboard](https://railway.app)
2. Выберите проект `plazma-production`
3. Нажмите **"+ New"** → **"Database"** → **"Add MongoDB"**
4. Дождитесь создания MongoDB сервиса (1-2 минуты)

### Шаг 2: Обновите DATABASE_URL

1. Откройте ваш основной сервис (бот) в Railway
2. Перейдите в **"Settings"** → **"Variables"**
3. Найдите переменную `DATABASE_URL`
4. Измените значение на: `${{MongoDB.MONGO_URL}}`
   - Нажмите **"Add Reference"**
   - Выберите `MongoDB` → `MONGO_URL`
   - Или вручную введите: `${{MongoDB.MONGO_URL}}`

### Шаг 3: Примените миграции

После обновления `DATABASE_URL` Railway автоматически перезапустит приложение. Затем примените миграции:

```bash
# Через Railway CLI
railway link
railway run npx prisma db push
```

Или через Railway Dashboard:
1. Откройте сервис
2. Перейдите в "Deployments"
3. Откройте терминал (если доступен)
4. Выполните: `npx prisma db push`

## 📊 Восстановление данных

После переключения на Railway MongoDB восстановите данные из бэкапа:

```bash
railway run npm run restore
```

Скрипт автоматически:
1. Найдет последний бэкап в Cloudinary
2. Скачает его
3. Восстановит все данные

**Подробная инструкция:** См. [RESTORE_FROM_BACKUP.md](./RESTORE_FROM_BACKUP.md)

## ✅ Проверка

После переключения проверьте логи:

```bash
railway logs
```

Ожидаемые сообщения:
- ✅ `Database URL configured: mongodb://mongo:27017...` (не `mongodb+srv://`)
- ✅ `Database connected`
- ✅ `Database schema synchronized` (или `already up to date`)

## 🔍 Как проверить текущий DATABASE_URL

В логах Railway найдите строку:
```
Database URL configured: mongodb+srv://PLAZMA...
```

Если видите `mongodb+srv://` - это MongoDB Atlas (старая база)
Если видите `mongodb://mongo:27017` - это Railway MongoDB (новая база) ✅

## 🐛 Решение проблем

### Проблема: "MongoDB service not found"

**Решение:**
1. Убедитесь, что MongoDB сервис создан в Railway
2. Проверьте имя сервиса (должно быть `MongoDB` или похожее)
3. В Reference Variable используйте правильное имя: `${{MongoDB.MONGO_URL}}`

### Проблема: "Reference Variable not working"

**Решение:**
1. Убедитесь, что MongoDB сервис запущен
2. Проверьте, что переменная `MONGO_URL` существует в MongoDB сервисе
3. Попробуйте скопировать значение `MONGO_URL` напрямую вместо Reference Variable

### Проблема: "Schema sync failed"

**Решение:**
1. Примените миграции вручную: `railway run npx prisma db push`
2. Проверьте подключение к базе: `railway run npx prisma db push --skip-generate`
3. Убедитесь, что база данных доступна

## 📝 Чеклист

- [ ] MongoDB сервис создан в Railway
- [ ] `DATABASE_URL` обновлен на `${{MongoDB.MONGO_URL}}`
- [ ] Приложение перезапущено
- [ ] Миграции применены (`npx prisma db push`)
- [ ] Данные восстановлены из бэкапа (`npm run restore`)
- [ ] Логи показывают подключение к Railway MongoDB
- [ ] Бот работает корректно

---

**💡 После переключения:** Бот будет работать быстрее и надежнее, так как база данных находится в той же инфраструктуре, что и приложение.

