# Интеграция Cloudinary для хранения изображений

## ✅ Что реализовано

### 1. Сервис Cloudinary (`src/services/cloudinary-service.ts`)
- ✅ Централизованная конфигурация Cloudinary из переменных окружения
- ✅ Загрузка изображений из буфера (`uploadImage`)
- ✅ Загрузка изображений из файла (`uploadImageFromPath`)
- ✅ Удаление изображений (`deleteImage`)
- ✅ Получение URL изображений с трансформациями (`getImageUrl`)
- ✅ Проверка конфигурации (`isCloudinaryConfigured`)
- ✅ Обработка ошибок и логирование

### 2. Интеграция в админ-панель (`src/admin/web.ts`)
- ✅ Загрузка изображений при создании товара
- ✅ Загрузка изображений при обновлении товара
- ✅ Отдельный роут для загрузки изображения товара (`/admin/products/:id/upload-image`)
- ✅ Отдельный роут для загрузки фото отзыва (`/admin/reviews/:id/upload-image`)
- ✅ Улучшенная обработка ошибок
- ✅ Валидация типов файлов (только изображения)
- ✅ Увеличен лимит размера файла до 10MB

### 3. Интеграция в сервисы (`src/services/admin/product-service.ts`)
- ✅ Реализован метод `uploadImage` в `ProductService`
- ✅ Использование централизованного сервиса Cloudinary

### 4. Конфигурация (`src/config/env.ts`)
- ✅ Добавлены переменные окружения для Cloudinary

## 📋 Настройка переменных окружения

Добавьте следующие переменные в Railway или ваш `.env` файл:

```bash
# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=ваш_cloud_name
CLOUDINARY_API_KEY=ваш_api_key
CLOUDINARY_API_SECRET=ваш_api_secret
```

### Как получить данные Cloudinary:

1. Зарегистрируйтесь на https://cloudinary.com (бесплатно до 25GB)
2. Перейдите в Dashboard
3. Скопируйте:
   - **Cloud Name** → `CLOUDINARY_CLOUD_NAME`
   - **API Key** → `CLOUDINARY_API_KEY`
   - **API Secret** → `CLOUDINARY_API_SECRET`

## 🚀 Использование

### В админ-панели:

1. **Загрузка фото товара:**
   - Перейдите в раздел "Товары"
   - Нажмите "📷 Добавить фото" или "📷 Изменить фото"
   - Выберите изображение (макс. 10MB)
   - Фото автоматически загрузится в Cloudinary

2. **Загрузка фото отзыва:**
   - Перейдите в раздел "Отзывы"
   - Нажмите "📷 Добавить фото" или "📷 Изменить фото"
   - Выберите изображение (макс. 10MB)
   - Фото автоматически загрузится в Cloudinary

### В коде:

```typescript
import { uploadImage, isCloudinaryConfigured } from '../services/cloudinary-service.js';

// Проверка конфигурации
if (!isCloudinaryConfigured()) {
  console.error('Cloudinary не настроен!');
  return;
}

// Загрузка изображения
const imageBuffer = /* ваш буфер изображения */;
const result = await uploadImage(imageBuffer, {
  folder: 'vital/products',
  publicId: 'product-123',
  resourceType: 'image',
});

console.log('Image URL:', result.secureUrl);
```

## 📁 Структура папок в Cloudinary

Изображения загружаются в следующие папки:
- `vital/products` - изображения товаров
- `vital/reviews` - фото отзывов
- `vital/` - другие изображения

## 🔧 Особенности

### Автоматическая оптимизация
Cloudinary автоматически оптимизирует изображения:
- Формат (WebP для браузеров с поддержкой)
- Качество (автоматическая настройка)
- Размер (уменьшение при необходимости)

### Безопасность
- Используется HTTPS (`secure: true`)
- Проверка типов файлов (только изображения)
- Ограничение размера файлов (10MB)
- Валидация переменных окружения

### Обработка ошибок
- Подробное логирование ошибок
- Понятные сообщения для пользователя
- Проверка конфигурации перед загрузкой

## 📝 Примеры использования API

### Загрузка с трансформациями:

```typescript
const result = await uploadImage(buffer, {
  folder: 'vital/products',
  transformation: [
    { width: 800, height: 600, crop: 'fill' },
    { quality: 'auto' },
    { format: 'auto' }
  ]
});
```

### Получение URL с трансформациями:

```typescript
import { getImageUrl } from '../services/cloudinary-service.js';

const url = getImageUrl('vital/products/product-123', [
  { width: 400, height: 300, crop: 'fill' },
  { quality: 'auto' }
]);
```

### Удаление изображения:

```typescript
import { deleteImage } from '../services/cloudinary-service.js';

await deleteImage('vital/products/product-123');
```

## ⚠️ Важные заметки

1. **Без переменных окружения:**
   - Сервис будет показывать предупреждение
   - Загрузка изображений будет невозможна
   - Приложение продолжит работать без загрузки изображений

2. **Лимиты:**
   - Максимальный размер файла: 10MB
   - Поддерживаемые форматы: все типы изображений (`image/*`)
   - Бесплатный план Cloudinary: до 25GB хранилища

3. **Производительность:**
   - Изображения загружаются асинхронно
   - Используется потоковая загрузка (`upload_stream`)
   - Результаты кэшируются Cloudinary

## 🔄 Миграция существующих данных

Если у вас уже есть изображения с URL, они будут работать как есть. Новые изображения будут загружаться в Cloudinary автоматически.

## 📚 Дополнительная информация

- [Документация Cloudinary](https://cloudinary.com/documentation)
- [Node.js SDK](https://cloudinary.com/documentation/node_integration)
- [Трансформации изображений](https://cloudinary.com/documentation/image_transformations)











