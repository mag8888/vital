# 🚀 Быстрое исправление для Railway

## Проблема
Сервис `vital` падает с ошибкой: `Environment variable DATABASE_URL is required`

## Решение (2 минуты)

### В Railway Dashboard:

1. **Откройте сервис "vital"** в Railway

2. **Перейдите на вкладку "Variables"**

3. **Добавьте DATABASE_URL через Reference:**
   - Нажмите **"+ New Variable"**
   - Нажмите **"Reference"**
   - Выберите **"MongoDB"** сервис
   - Выберите переменную **`MONGO_PUBLIC_URL`**
   - Назовите её **`DATABASE_URL`**
   - Сохраните

4. **Добавьте обязательные переменные:**
   - **`ADMIN_EMAIL`** - ваш email (например: `admin@vital.com`)
   - **`ADMIN_PASSWORD`** - надежный пароль для админ-панели

5. **Перезапустите сервис:**
   - Deployments → **"Redeploy"**

---

## Итого нужно добавить:

```
DATABASE_URL     → Reference на MONGO_PUBLIC_URL из MongoDB
ADMIN_EMAIL      → ваш email
ADMIN_PASSWORD   → ваш пароль
```

**✅ Готово! Бот должен запуститься.**









