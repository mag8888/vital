# 🚀 Развертывание на Railway

## Настройка для домена vital-production.up.railway.app

### 1. Переменные окружения в Railway

Добавьте следующие переменные в настройках Railway:

```env
BOT_TOKEN=your_bot_token_here
PUBLIC_BASE_URL=https://vital-production.up.railway.app
WEBAPP_URL=https://vital-production.up.railway.app/webapp
DATABASE_URL=your_mongodb_connection_string
SESSION_SECRET=your_session_secret_key
```

### 2. URL веб-приложения

После развертывания веб-приложение будет доступно по адресу:
```
https://vital-production.up.railway.app/webapp
```

### 3. Настройка кнопки в боте

После развертывания выполните настройку кнопки меню:

```bash
# На сервере Railway или локально
npm run setup-webapp
```

Или вручную через BotFather:
1. Откройте @BotFather в Telegram
2. Выберите вашего бота
3. Выберите "Menu Button"
4. Введите URL: `https://vital-production.up.railway.app/webapp`
5. Введите текст кнопки: `🌐 Веб-приложение`

### 4. Проверка работоспособности

После развертывания проверьте:

1. **Health Check:**
   ```
   GET https://vital-production.up.railway.app/health
   ```

2. **Web App:**
   ```
   GET https://vital-production.up.railway.app/webapp
   ```

3. **API Health:**
   ```
   GET https://vital-production.up.railway.app/webapp/api/health
   ```

### 5. Логи Railway

Для отладки проверьте логи в Railway Dashboard:
- Откройте проект в Railway
- Перейдите в раздел "Deployments"
- Выберите последний деплой
- Просмотрите логи для выявления ошибок

### 6. Автоматическое развертывание

Railway автоматически развернет приложение при пуше в main ветку:
```bash
git push origin main
```

### 7. Проверка SSL

Railway автоматически предоставляет SSL сертификат для домена `.up.railway.app`.

### 8. Настройка домена

Если нужно использовать собственный домен:
1. В Railway Dashboard добавьте кастомный домен
2. Обновите переменные окружения:
   ```env
   PUBLIC_BASE_URL=https://your-custom-domain.com
   WEBAPP_URL=https://your-custom-domain.com/webapp
   ```
3. Перезапустите приложение

### 9. Мониторинг

Railway предоставляет:
- Автоматические деплои
- Логи в реальном времени
- Метрики производительности
- Автоматическое масштабирование

### 10. Отладка

При возникновении проблем:

1. **Проверьте логи:**
   ```bash
   # В Railway Dashboard или через CLI
   railway logs
   ```

2. **Проверьте переменные окружения:**
   ```bash
   railway variables
   ```

3. **Перезапустите приложение:**
   ```bash
   railway redeploy
   ```

## 🎯 Результат

После успешного развертывания:
- ✅ Веб-приложение доступно по `https://vital-production.up.railway.app/webapp`
- ✅ Кнопка "🌐 Веб-приложение" появится в боте
- ✅ Все API endpoints работают
- ✅ SSL сертификат активен
- ✅ Автоматические деплои настроены
