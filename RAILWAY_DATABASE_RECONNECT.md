# 🔌 Переподключение MongoDB в Railway

## Проблема

Ошибка `SCRAM failure: Authentication failed` обычно возникает, когда `DATABASE_URL` не правильно настроен или использует старые учетные данные.

## Решение: Использовать Variable Reference

В Railway нужно использовать **Variable Reference** (ссылку на переменную), а не копировать значение напрямую.

### Шаг 1: Откройте настройки сервиса `vital`

1. В Railway Dashboard откройте сервис **`vital`** (не MongoDB!)
2. Перейдите во вкладку **Variables**

### Шаг 2: Добавьте/обновите DATABASE_URL

1. Найдите переменную `DATABASE_URL` в списке (если она есть)
2. Если её нет — нажмите **"+ New Variable"**
3. В поле **Name** введите: `DATABASE_URL`
4. В поле **Value** НЕ вводите URL напрямую!
5. Вместо этого нажмите на иконку **"Reference"** (ссылка) справа от поля Value
6. Выберите:
   - **Service:** `MongoDB`
   - **Variable:** `MONGO_PUBLIC_URL`

Или используйте синтаксис ссылки напрямую:
```
{{MongoDB.MONGO_PUBLIC_URL}}
```

### Шаг 3: Сохраните и перезапустите

1. Нажмите **"Add"** или **"Update"**
2. Railway автоматически перезапустит сервис
3. Или нажмите **"Redeploy"** вручную

### Шаг 4: Проверьте логи

1. Перейдите во вкладку **Deployments**
2. Откройте последний деплой → **View Logs**
3. Должны увидеть:
   - ✅ `Database connected`
   - ✅ `Bot launched successfully`
   - ❌ НЕ должно быть: `Authentication failed`

## Альтернативный способ (через UI)

1. В сервисе **MongoDB** → вкладка **Variables**
2. Найдите переменную `MONGO_PUBLIC_URL`
3. Нажмите на иконку **"🔗"** (ссылка) рядом с ней
4. Выберите сервис **`vital`**
5. Выберите переменную `DATABASE_URL` или создайте новую
6. Сохраните

## Проверка подключения

После переподключения проверьте:

1. **В сервисе `vital` → Variables:**
   - `DATABASE_URL` должна быть зелёной (это значит, что это ссылка)
   - Значение должно быть вида: `{{MongoDB.MONGO_PUBLIC_URL}}`

2. **В логах сервиса `vital`:**
   - ✅ `Database URL configured: mongodb://...`
   - ✅ `Database connected`
   - ✅ `Bot launched successfully`

## Если проблема осталась

1. **Проверьте, что MongoDB сервис запущен:**
   - В MongoDB сервисе должен быть зелёный статус

2. **Сбросьте пароль MongoDB:**
   - MongoDB сервис → Settings → Reset Password
   - Railway сгенерирует новый пароль
   - `MONGO_PUBLIC_URL` автоматически обновится
   - Сервис `vital` автоматически получит новый URL через ссылку

3. **Убедитесь, что используется ссылка, а не копия:**
   - В Variables сервиса `vital`
   - `DATABASE_URL` должна иметь иконку ссылки или показывать `{{MongoDB.MONGO_PUBLIC_URL}}`
   - Если там просто текст URL — удалите и создайте заново через Reference



