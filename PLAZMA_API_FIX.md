# 🔧 Исправление интеграции Plazma API

## Проблема

Plazma API возвращал 404 для endpoint `/products`, что приводило к тому, что товары не отображались.

## Решение

Добавлен fallback механизм:
1. Сначала пробуем endpoint `/products`
2. Если получаем 404, используем `/catalog`
3. Извлекаем товары из категорий, если используем `/catalog`

## Проверка

После деплоя проверьте:

1. **Тестовый endpoint:**
   ```
   https://vital-production-82b0.up.railway.app/webapp/api/plazma/test
   ```
   Должен показать, что API ключ настроен.

2. **Получение товаров:**
   ```
   https://vital-production-82b0.up.railway.app/webapp/api/plazma/products?limit=5
   ```
   Теперь должен вернуть товары, используя `/catalog` если `/products` недоступен.

3. **Проверка в браузере:**
   - Откройте веб-приложение
   - Прокрутите вниз
   - Должна появиться секция "Рекомендуем" с товарами из Plazma

## Логи

В логах сервера вы увидите:
- `🔗 Fetching Plazma products from:` - URL запроса
- Если `/products` недоступен: `⚠️ /products endpoint not found, trying /catalog...`
- `📦 Plazma API response:` - структура ответа
- `✅ Parsed X products from Plazma API` - количество товаров

## Формат данных

### При использовании `/catalog`:
```json
{
  "success": true,
  "data": [
    {
      "products": [
        { ...товар... }
      ]
    }
  ]
}
```

### При использовании `/products`:
```json
{
  "success": true,
  "data": [
    { ...товар... }
  ]
}
```

Код автоматически обрабатывает оба формата! ✅











