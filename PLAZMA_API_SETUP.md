# Интеграция с Plazma API - Инструкция

## ✅ Настройка завершена

Вы добавили переменную окружения `PLAZMA_API_KEY`. Система автоматически подхватит её при следующем запуске.

## 🔧 Что уже настроено

### 1. Конфигурация
- Переменная окружения: `PLAZMA_API_KEY` или `EXTERNAL_API_KEY`
- URL API (по умолчанию): `https://plazma-production.up.railway.app/api/external`
- Можно изменить через переменную: `PLAZMA_API_URL`

### 2. API Endpoints

Система предоставляет следующие endpoints для работы с Plazma API:

#### Получить список товаров
```
GET /webapp/api/plazma/products?region=RUSSIA&limit=20
```

#### Получить конкретный товар
```
GET /webapp/api/plazma/products/:id
```

#### Создать заказ через Plazma API
```
POST /webapp/api/plazma/orders
Body: {
  productId: string,
  productTitle: string,
  price: number,
  quantity?: number
}
```

## 🎯 Как это работает

### На главной странице веб-приложения:

1. **Первая строка товаров** — отображает товары из вашей базы данных (Vital)
   - Горизонтальный скролл
   - Заголовок: "Каталог"

2. **Вторая строка товаров** — отображает товары из Plazma API
   - Горизонтальный скролл
   - Заголовок: "Рекомендуем"
   - Загружается автоматически при открытии страницы
   - Если API недоступен или ключ не настроен — секция скрывается

### При создании заказа:

- Когда пользователь выбирает товар из Plazma API
- Заказ автоматически создается через Plazma API
- Также сохраняется в вашей базе данных для отслеживания

## 📋 Проверка работы

### 1. Проверьте переменную окружения

В Railway или локальном `.env` файле должна быть:
```
PLAZMA_API_KEY=ваш-ключ-здесь
```

### 2. Перезапустите сервер

Если сервер уже запущен, перезапустите его, чтобы переменная окружения подхватилась:
```bash
# В Railway это произойдет автоматически при деплое
# Локально:
npm run dev
```

### 3. Проверьте логи

При запуске сервера проверьте логи. Если API ключ не найден, вы увидите:
```
⚠️ Plazma API key not configured
```

Если всё правильно, никаких предупреждений не будет.

### 4. Откройте веб-приложение

1. Откройте главную страницу веб-приложения
2. Прокрутите вниз после первой строки товаров
3. Должна появиться вторая строка "Рекомендуем" с товарами из Plazma API

## 🐛 Решение проблем

### Товары из Plazma API не отображаются

1. **Проверьте переменную окружения:**
   - Убедитесь, что `PLAZMA_API_KEY` установлена
   - Проверьте правильность ключа

2. **Проверьте логи сервера:**
   - Ищите ошибки типа "Plazma API error"
   - Проверьте, что API ключ валидный

3. **Проверьте доступность API:**
   - Убедитесь, что `https://plazma-production.up.railway.app/api/external` доступен
   - Можно проверить в браузере с заголовком `X-API-Key`

4. **Секция скрывается автоматически:**
   - Если товаров нет или API недоступен — секция не показывается
   - Это нормальное поведение

### API возвращает ошибку 401

- Проверьте правильность API ключа
- Убедитесь, что ключ активирован на стороне Plazma

### API возвращает ошибку 503

- Проверьте доступность Plazma API
- Убедитесь, что URL правильный (`PLAZMA_API_URL`)

## 📚 Дополнительная информация

Подробная документация по Plazma API находится в файле:
- `EXTERNAL_PLAZMA_API.md`

## ✨ Готово!

Теперь ваше веб-приложение автоматически будет:
- Загружать товары из Plazma API
- Отображать их во второй строке на главной странице
- Позволять пользователям создавать заказы через Plazma API

Все работает автоматически — ничего дополнительного настраивать не нужно!











